# attendancem — アーキテクチャ決定記録（ADR）

本ファイルは attendancem の設計上・実装上の意思決定を時系列で記録する台帳である。
「なぜそうなっているのか」を後から辿れるようにすることが唯一の目的であり、
仕様書（[PLAN.md](./PLAN.md)）や手順書（README.md）とは役割が異なる。

---

## このファイルの書き方（運用ルール）

一般的な ADR は「1決定 = 1ファイル」で運用するが、本プロジェクトは小規模のため
**単一ファイルに連番レコードを追記していく**方式を採る。以下のルールに従う。

1. **1レコード = 1決定。** 複数の決定を1つのレコードに詰め込まない。
2. **レコードは追記のみ。過去のレコードの本文は書き換えない。**
   決定を覆す場合は、既存レコードを編集せず新しいレコードを追加し、
   旧レコードのステータスを `置換済み（ADR-00XX により置換）` に変更する。
   ステータス行の更新だけが、既存レコードへの唯一の許容される編集である。
3. **番号は連番。欠番・再利用をしない。**
4. **「決定」は断定形・能動態で書く。**
   「〜を検討する」ではなく「〜を採用する」。
5. **検討した代替案と、採用しなかった理由を必ず残す。**
   ADR の価値の大半はここにある。「他の選択肢を知らなかった」のか
   「知った上で捨てた」のかが後から区別できるようにする。
6. **影響（Consequences）には不都合な結果も書く。**
   良い面だけを書いた ADR は、後から読む人を誤導する。
7. **日付は絶対日付で書く。**「先週」「今後」のような相対表現を使わない。
8. **設計時の決定だけでなく、実装中に生じた判断も同じ形式で追記する。**
   「実装してみたら前提が違った」は最も価値の高い記録である。
9. **記録する対象は「後から変更するのが高くつく判断」に限る。**
   変数名やインデント幅のような、いつでも戻せる判断は記録しない。

### ステータスの定義

| ステータス | 意味 |
|-----------|------|
| 提案中 | 議論中。まだ実装の根拠にしてはならない |
| 承認済み | 有効。現在の設計の根拠 |
| 棄却 | 提案されたが採用されなかった |
| 置換済み | かつて有効だったが、後続の ADR に取って代わられた |

### レコードのテンプレート

```markdown
<a id="adr-00XX"></a>
### ADR-00XX: <決定を一文で>

- **ステータス**: 提案中 / 承認済み / 棄却 / 置換済み（ADR-00YY により置換）
- **日付**: YYYY-MM-DD
- **区分**: 設計時 / 実装中
- **関連**: ADR-00YY, PLAN.md §N

**背景**
どういう状況・制約があってこの判断が必要になったか。事実のみを書く。

**決定**
何を採用するか。断定形で書く。

**理由**
なぜその選択肢なのか。

**検討した代替案**
| 案 | 却下理由 |
|----|---------|

**影響**
- 良い影響: 
- 悪い影響 / トレードオフ: 
- 再検討のトリガー: どうなったらこの決定を見直すか
```

---

## 索引

| # | 決定 | ステータス | 日付 | 区分 |
|---|------|-----------|------|------|
| [0001](#adr-0001) | 実行基盤に Google Apps Script Web App を採用する | 承認済み | 2026-08-05 | 設計時 |
| [0002](#adr-0002) | 認証を Web App の「自分として実行」に委譲し、独自 OAuth 実装を持たない | 承認済み | 2026-08-05 | 設計時 |
| [0003](#adr-0003) | 打刻を「開始」「終了」の独立した2イベントとして記録する | 承認済み | 2026-08-05 | 設計時 |
| [0004](#adr-0004) | 招待者への通知として毎回招待メールを送信する | 置換済み（ADR-0020 により置換） | 2026-08-05 | 設計時 |
| [0005](#adr-0005) | フロントエンドを HtmlService で同居配信し `google.script.run` で呼び出す | 承認済み | 2026-08-05 | 設計時 |
| [0006](#adr-0006) | 二重打刻を当日イベント検索と LockService の二段で防ぐ | 置換済み（ADR-0018 により置換） | 2026-08-05 | 設計時 |
| [0007](#adr-0007) | タイムゾーンをマニフェストで Asia/Tokyo に固定する | 承認済み | 2026-08-05 | 設計時 |
| [0008](#adr-0008) | 打刻イベントの長さを1分とする | 承認済み | 2026-08-05 | 設計時 |
| [0009](#adr-0009) | 招待先メールアドレスを Script Properties で保持する | 承認済み | 2026-08-05 | 設計時 |
| [0010](#adr-0010) | 集計・レポート機能を実装せず AI に委ねる | 承認済み | 2026-08-05 | 設計時 |
| [0011](#adr-0011) | 打刻に確認ダイアログを設けず、ボタン間隔と復旧手順で誤タップに対応する | 置換済み（ADR-0019 により置換） | 2026-08-05 | 実装中 |
| [0012](#adr-0012) | clasp をプロジェクトローカルに導入し、認証情報もリポジトリ内に閉じる | 承認済み | 2026-08-05 | 実装中 |
| [0013](#adr-0013) | OAuth スコープを `calendar` のみに限定し、マニフェストで明示宣言する | 置換済み（ADR-0014 により置換） | 2026-08-05 | 実装中 |
| [0014](#adr-0014) | OAuth スコープを `calendar` のみに限定する（git 履歴で原本と一致を検証済み） | 承認済み | 2026-08-05 | 実装中 |
| [0015](#adr-0015) | セットアップ手順を AI Agent 向けに SETUP.md として文書化し、マニフェスト上書きを回避で防ぐ | 承認済み | 2026-08-05 | 実装中 |
| [0016](#adr-0016) | 本番反映は既存デプロイのバージョン更新で行い、Web App URL を固定する | 承認済み | 2026-08-05 | 実装中 |
| [0017](#adr-0017) | 打刻種別を6種に拡張し、出社形態を独立したイベントタイトルで表現する | 置換済み（ADR-0023 により置換） | 2026-08-05 | 設計時 |
| [0018](#adr-0018) | 同日複数回の打刻を許可し、重複禁止を状態遷移チェックに置き換える | 承認済み | 2026-08-05 | 設計時 |
| [0019](#adr-0019) | 誤タップ対策の主軸を、ボタンの物理的配置から状態遷移チェックと連打しきい値に移す | 承認済み | 2026-08-05 | 設計時 |
| [0020](#adr-0020) | 招待メールの送信可否を打刻種別ごとに分け、休憩は招待のみで送信しない | 承認済み | 2026-08-05 | 設計時 |
| [0021](#adr-0021) | 勤務状態の遷移表だけを純粋関数に切り出し、GAS 上の簡易テストで網羅する | 承認済み | 2026-08-05 | 設計時 |
| [0022](#adr-0022) | 6つの打刻ボタンを2列3行のグリッドに配置し、88px の寸法を維持する | 承認済み | 2026-08-05 | 実装中 |
| [0023](#adr-0023) | 打刻タイトルを `{種別名}_{氏名}` とし、種別判定を最初の `_` の前の完全一致に置き換える | 承認済み | 2026-08-06 | 設計時 |
| [0024](#adr-0024) | 氏名を画面からの入力で受け取り、User Properties に保存する | 承認済み | 2026-08-06 | 設計時 |

---

<a id="adr-0001"></a>
### ADR-0001: 実行基盤に Google Apps Script Web App を採用する

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0002, ADR-0005, PLAN.md §3

**背景**

個人の Google アカウント（Gmail）のカレンダーに、招待者付きの予定を作成する必要がある。
この用途には2つの構造的な制約がある。

1. **サービスアカウントが使えない。** Google Calendar API でサービスアカウントが
   `attendees` を含むイベントを作成しようとすると `forbiddenForServiceAccounts` で
   拒否される。回避には Google Workspace の「ドメイン全体の委任」が必要であり、
   個人 Gmail アカウントでは設定できない。したがってユーザー OAuth が必須になる。
2. **OAuth トークンの維持が難しい。** Calendar スコープは Google の「機密スコープ」に
   分類される。OAuth 同意画面を「テスト」ステータスのまま運用すると
   リフレッシュトークンが7日で失効し、毎週再ログインが必要になる。
   これを避けるには Google の審査を通した本番公開が必要だが、
   個人用ツールに対して手続きが重すぎる。

**決定**

Google Apps Script の Web App を実行基盤として採用する。

**理由**

Web App を「自分として実行（`USER_DEPLOYING`）」でデプロイすると、
スクリプト自身がオーナーの認可でカレンダーを操作する。
この認可は上記2の7日失効の対象外であり、**制約1と2が設計から消える**。
加えてホスティング・実行環境が無料で、サーバー構築もデプロイパイプラインも不要。

要件は1日2回のボタン操作であり、UI の作り込みや実行環境の柔軟性より
「認証が壊れないこと」「維持コストがゼロであること」の価値が圧倒的に高い。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| Vite + React + Google Identity Services（クライアントのみ） | 認証が最大の課題なのに、その課題を自前で抱え込む構成。都度同意フローか7日失効のどちらかを受け入れる必要がある。UI の自由度という利得は、本要件（ボタン2個）では価値が小さい |
| Next.js + DB でリフレッシュトークンを保管 | 本番公開の審査が必要。DB・ホスティングの運用コストが発生。ボタン2個のツールに対して過剰 |
| サービスアカウント | 個人 Gmail では招待者付きイベントを作成できない。技術的に不可能 |
| Zapier / Make などのノーコード | 二重打刻防止のような条件分岐ロジックが組みにくい。Webhook トリガーは実質有料 |
| iOS ショートカット単体 | 標準のカレンダーアクションに招待者を指定する手段がなく、単体では要件を満たせない |

**影響**

- 良い影響: 認証まわりのコードがゼロになる。運用コスト0円。実装が数十行に収まる
- 悪い影響 / トレードオフ:
  - GAS のエディタ／`clasp` に開発体験が縛られる。npm エコシステムが使えない
  - UI は素の HTML/CSS/JS になる（React 等のフレームワークは実質使えない）
  - Web App は iframe サンドボックス内で描画されるため、PWA としての挙動に制限がある
- 再検討のトリガー: 複数人で使う必要が出た場合、または UI が HTML 手書きで管理しきれない規模になった場合

---

<a id="adr-0002"></a>
### ADR-0002: 認証を Web App の「自分として実行」に委譲し、独自 OAuth 実装を持たない

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0001, PLAN.md §2.2 N-4/N-7

**背景**

ADR-0001 で GAS を採用したが、Web App のデプロイ設定には
「実行するユーザー」と「アクセスできるユーザー」の2軸があり、
組み合わせによってセキュリティ特性が大きく変わる。

**決定**

`appsscript.json` の `webapp` を以下で固定する。

- `executeAs: "USER_DEPLOYING"`（自分として実行）
- `access: "MYSELF"`（自分のみアクセス可）

アプリケーション独自の認証（トークン、パスワード、セッション）は一切実装しない。

**理由**

`access: "MYSELF"` により、URL が漏れても Google アカウント認証で弾かれる。
自作の認証を持たないということは、自作の認証バグを持たないということでもある。
非機能要件 N-7（自分以外からアクセスできない）はこれで満たされる。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| `access: "ANYONE_ANONYMOUS"` + シークレットトークン検証 | URL さえ知られれば、あとは自作のトークン検証だけが防壁になる。今回は不要なリスク。ただし将来 iOS ショートカットから叩く場合はこの方式が必要になる（→ PLAN.md §8） |
| `executeAs: "USER_ACCESSING"`（アクセスしたユーザーとして実行） | 利用者は自分だけなので差が出ない上、アクセスのたびに OAuth 同意が必要になり N-4 を損なう |

**影響**

- 良い影響: 認証コードがゼロ。秘密情報をソースにもリクエストにも載せない
- 悪い影響 / トレードオフ:
  - ブラウザで Google にログインしていることが前提になる
  - **API として外部から叩けない。** iOS ショートカットや他アプリからの呼び出しは
    この設定のままでは不可能
- 再検討のトリガー: ホーム画面からの2タップでは不満が出て、ショートカット等からの
  ワンアクション実行が必要になった場合（そのとき ADR-0002 を置換する新レコードを起こす）

---

<a id="adr-0003"></a>
### ADR-0003: 打刻を「開始」「終了」の独立した2イベントとして記録する

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0008, PLAN.md §2.1 F-1/F-2

**背景**

打刻の表現方法には2案がある。

- A案: 「開始」「終了」という短いイベントを、押下のたびに1件ずつ作る（当初要件）
- B案: 開始時に1件のイベントを作り、終了時にそのイベントの終了時刻を更新して伸ばす

B案には、カレンダー上で稼働時間が帯として可視化される、集計が終了時刻−開始時刻の
差分で済む、招待メールが1日1回で済む、という利点がある。
設計相談の場でこのトレードオフを提示した。

**決定**

A案を採用し、「開始」「終了」を独立した2件のイベントとして作成する。

**理由**

利用者が明示的に A案を選択した。
打刻を「点」として記録したいという要件は、稼働時間の可視化とは別の価値である。
また A案は各打刻が独立しているため、片方だけの打刻・打刻漏れが
そのままカレンダー上で欠落として見え、状態管理も単純になる。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| B案（1件のイベントを伸ばす） | 利用者が A案を選択。またイベント更新には作成後のイベント ID の追跡が必要になり、状態管理が A案より複雑になる |

**影響**

- 良い影響: 実装が単純。各打刻が独立しているため、片方の失敗が他方に影響しない
- 悪い影響 / トレードオフ:
  - 稼働時間はカレンダーを見ただけでは分からない（開始と終了のペアリングが必要）
  - 招待メールが1日2通になる（→ ADR-0004）
  - カレンダー上のイベント数が B案の2倍になる
- 再検討のトリガー: 稼働時間の可視化・集計の要求が強くなった場合

---

<a id="adr-0004"></a>
### ADR-0004: 招待者への通知として毎回招待メールを送信する

- **ステータス**: 置換済み（ADR-0020 により置換）
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0003, PLAN.md §7

**背景**

Google Calendar のイベント作成時、招待者への通知は制御できる。

- `sendInvites: true` — 招待メールが送信される
- `sendInvites: false` — メールは送信されないが、招待者のカレンダーには予定として表示される
  （個人 Gmail の既定設定では招待は自動的にカレンダーに追加されるため）

ADR-0003 により1日2件のイベントが発生するため、`true` の場合の年間メール通数は
稼働日を250日とすると約500通になる。この負担を設計相談の場で明示的に提起した。

**決定**

`sendInvites: true` を採用し、打刻のたびに招待メールを送信する。
ただしこのフラグは設定として1箇所に集約し、コード全体を横断せずに切り替えられる構造とする。

**理由**

利用者が、通知の確実性を優先して毎回のメール送信を選択した。
招待先がカレンダーを日常的に見ない運用である場合、メールが届かなければ
共有の目的そのものが達成されないため、この判断には合理性がある。

なお「相手にとって通知過多になりうる」という懸念は提起済みであり、
利用者はそれを理解した上でこの選択をしている。実運用で問題が出た場合に
即座に切り替えられるよう、フラグの集約だけを設計上の保険として組み込む。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| `sendInvites: false`（メールなし・カレンダーには表示） | メール通数はゼロになるが、招待先がカレンダーを見ない運用だと共有が成立しない。利用者が通知の確実性を優先した |
| 招待をやめてカレンダー自体を2名に共有 | イベントがシンプルになり通知も発生しないが、要件は「予定に招待する」ことであり、要件そのものの変更にあたる |

**影響**

- 良い影響: 招待先が確実に気づく。カレンダーを見る習慣がなくても共有が成立する
- 悪い影響 / トレードオフ:
  - 招待先2名に年間約500通の招待メールが届く
  - 招待先のメールボックスが打刻通知で埋まり、重要な連絡が埋没するおそれがある
- 再検討のトリガー: 招待先から通知量について苦情や要望があった場合。
  その際は設定フラグを `false` に変更し、本 ADR を置換するレコードを起こす

---

<a id="adr-0005"></a>
### ADR-0005: フロントエンドを HtmlService で同居配信し `google.script.run` で呼び出す

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0001, ADR-0002, PLAN.md §4.1

**背景**

GAS をバックエンドにする場合、フロントエンドの配信方法に2案ある。

- A案: `HtmlService` で GAS 自身が HTML を配信し、`google.script.run` でサーバー関数を呼ぶ
- B案: 別のホスト（Cloudflare Pages 等）に SPA を置き、GAS の `doPost` を `fetch` で叩く

B案には既知の落とし穴がある。ブラウザから `script.google.com` へ
`Content-Type: application/json` で POST すると CORS プリフライトが発生し、
GAS 側がこれに応答しないためリクエストが失敗する。
回避には `Content-Type: text/plain` を使い、サーバー側で JSON を手動パースする必要がある。
さらに ADR-0002 の `access: "MYSELF"` とは両立せず、匿名アクセス＋独自トークン検証が必要になる。

**決定**

A案を採用する。`doGet()` が `HtmlService` で `index.html` を返し、
クライアントからは `google.script.run` でサーバー関数を呼び出す。

**理由**

同一オリジンで完結するため CORS が存在しない。
`google.script.run` は認証済みセッション上で動くため、独自トークンも不要。
ADR-0002 のアクセス制御をそのまま活かせる。
つまり「CORS 回避策」と「独自トークン検証」という2つの複雑さが同時に消える。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| B案（外部ホスト + `doPost`） | CORS 回避策と独自認証が必要になり、ADR-0002 のアクセス制御も使えなくなる。得られるのは UI の自由度のみで、ボタン2個には見合わない |

**影響**

- 良い影響: CORS 設定・トークン管理・ビルド工程のすべてが不要。デプロイが1操作で完結
- 悪い影響 / トレードオフ:
  - `google.script.run` はコールバック方式のため、Promise でのラップが必要
  - GAS の Web App は iframe 内で描画されるため、画面遷移や PWA 機能に制約がある
  - HTML/CSS/JS を1ファイルに同梱するため、UI が肥大すると保守しにくい
- 再検討のトリガー: UI が1ファイルで管理しきれない規模になった場合

---

<a id="adr-0006"></a>
### ADR-0006: 二重打刻を当日イベント検索と LockService の二段で防ぐ

- **ステータス**: 置換済み（ADR-0018 により置換）
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: PLAN.md §2.1 F-5, §6 T-3/T-4

**背景**

打刻データの整合性を壊す最大の要因は「開始」が2件できることである。
発生経路は2つある。

1. 利用者がボタンを連打する
2. 通信が不安定な状況でリトライが走る

カレンダーが唯一の記録先であり、重複が発生すると集計時に
どちらが正なのか判断できなくなる。事後の手作業修正が必要になる。

**決定**

サーバー側で以下の二段構えを実装する。

1. `LockService.getScriptLock()` で排他制御し、`punch()` の処理を直列化する
2. ロック取得後、当日 0:00〜24:00 のイベントを検索し、
   同名（`開始` / `終了`）のイベントが既に存在すれば作成せずエラーを返す

加えてクライアント側でも、送信中はボタンを非活性にする（多層防御の一層目）。

**理由**

クライアント側の抑止だけでは不十分である。ページを2つ開く、リロード直後に押す、
といった経路で容易に回り込めるため、サーバー側に真の防壁を置く必要がある。

また存在チェックだけでも不十分である。ほぼ同時の2リクエストは、
どちらも「存在しない」と判定してから両方が作成に進む競合状態を起こしうる。
LockService で直列化して初めて、存在チェックが意味を持つ。

判定キーはイベントのタイトルとする。カレンダーが唯一の記録先であるため、
別途の状態ストアを持たずに済み、カレンダー側を手で修正した場合にも
ツールの認識が自動的に追従する。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| クライアント側の非活性化のみ | ページ複数開き・リロードで回避できてしまう。防壁として成立しない |
| Script Properties に打刻状態を保存して判定 | カレンダー側を手で修正したときに状態が乖離する。真実の源が二重化する |
| 存在チェックのみ（ロックなし） | ほぼ同時のリクエストで競合状態が発生し、重複を許してしまう |

**影響**

- 良い影響: 重複が構造的に発生しない。カレンダーが単一の真実の源であり続ける
- 悪い影響 / トレードオフ:
  - 打刻のたびに当日イベントの検索が走り、応答が数百ミリ秒遅くなる（N-1 の3秒には十分収まる）
  - 同名の予定を別目的で手動作成すると、打刻済みと誤判定される
  - 1日1回しか打刻できない。中抜け等で2回打刻したい運用には対応できない
- 再検討のトリガー: 1日に複数回の打刻が必要になった場合

---

<a id="adr-0007"></a>
### ADR-0007: タイムゾーンをマニフェストで Asia/Tokyo に固定する

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0006, PLAN.md §2.2 N-6, §6 T-6/T-7

**背景**

GAS プロジェクトのタイムゾーンは `appsscript.json` の `timeZone` で決まり、
これが既定と異なると `new Date()` の解釈や日付境界の計算がずれる。

ADR-0006 の二重打刻防止は「当日 0:00〜24:00」という日付境界に依存しているため、
タイムゾーンのずれは直接「深夜の打刻が前日/翌日に記録される」というバグになる。
この種の不具合は、ずれた時間帯に実際に操作するまで表面化しない。

**決定**

`appsscript.json` に `"timeZone": "Asia/Tokyo"` を明示する。
時刻の文字列化は必ず `Utilities.formatDate(date, 'Asia/Tokyo', ...)` で
タイムゾーンを明示的に指定し、暗黙のロケール依存に頼らない。

**理由**

明示は無料であり、暗黙の既定に頼ると再現の難しいバグを踏む。
既定値に依存した実装は、プロジェクトを複製したときに壊れる。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| GAS の既定タイムゾーンに任せる | 既定が意図と異なる場合に日付境界がずれ、深夜帯の打刻でのみ再現する不具合になる。検証コストが明示のコストを大きく上回る |
| すべて UTC で保持し表示時に変換 | 記録先が Google カレンダーであり、UI もサーバーも単一タイムゾーンで完結する。抽象化が要件に対して過剰 |

**影響**

- 良い影響: 日付境界の計算が確定する。深夜帯の打刻も正しい日付に記録される
- 悪い影響 / トレードオフ: 海外からの利用時、現地時間ではなく JST で記録される
- 再検討のトリガー: 複数タイムゾーンをまたぐ運用が発生した場合

---

<a id="adr-0008"></a>
### ADR-0008: 打刻イベントの長さを1分とする

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0003

**背景**

ADR-0003 により打刻は「点」を表すイベントになるが、
Google カレンダーのイベントは開始時刻と終了時刻を必要とする。
開始と終了を同一時刻にした長さ0のイベントは、カレンダー上での表示が不安定になる。

**決定**

打刻イベントの長さを1分（開始時刻 + 60,000ミリ秒）とする。

**理由**

意味的には点でありながら、カレンダー上で確実に描画される最小の長さである。
長すぎる（例: 15分）と、実際には存在しない時間帯を占有しているように見え、
他の予定との重なり判定にも影響する。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| 長さ0（開始=終了） | カレンダー上の表示が不安定 |
| 15分 | 実在しない時間帯を占有しているように見える。空き時間の判定に影響する |
| 終日イベント | 打刻時刻という最も重要な情報が失われる |

**影響**

- 良い影響: カレンダー上で確実に見え、かつ時間帯をほとんど占有しない
- 悪い影響 / トレードオフ: 厳密には打刻時刻に1分の幅がある。
  ただし記録として参照するのは開始時刻であり、実害はない
- 再検討のトリガー: なし（変更コストが極めて低いため、必要になった時点で変えればよい）

---

<a id="adr-0009"></a>
### ADR-0009: 招待先メールアドレスを Script Properties で保持する

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: PLAN.md §2.1 F-9, §4.2

**背景**

招待先の Gmail アドレス2件をどこに置くかを決める必要がある。
これは第三者の個人情報であり、かつ将来変更されうる値である。

**決定**

Script Properties のキー `GUEST_EMAILS` に、カンマ区切りで保持する。
ソースコードには実アドレスを一切含めない。
未設定時は、打刻を実行せず設定を促すエラーメッセージを返す。

**理由**

- ソースコードに他人のメールアドレスを残さずに済む
- 変更時にコードの修正・再デプロイが不要になる（F-9 を満たす）
- GAS に標準で備わる仕組みであり、追加の依存が発生しない

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| ソースコード内の定数 | 実装は最も簡単だが、他人のメールアドレスがコードに残る。変更のたびに再デプロイが必要 |
| UI 上で設定・保存できるようにする | 設定画面と永続化の実装が必要。年に数回も変わらない値に対して過剰 |

**影響**

- 良い影響: ソースコードに個人情報が残らない。設定変更が再デプロイ不要
- 悪い影響 / トレードオフ:
  - 初回セットアップに「Script Properties を設定する」という手順が増える。
    この手順を忘れると打刻が失敗するため、エラーメッセージで明示的に誘導する必要がある
  - 設定値がソースと別管理になり、プロジェクトを複製したときに引き継がれない
- 再検討のトリガー: 設定項目が増えて、プロパティの手動管理が煩雑になった場合

---

<a id="adr-0010"></a>
### ADR-0010: 集計・レポート機能を実装せず AI に委ねる

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: PLAN.md §1, §2.3

**背景**

「打刻を記録する」ツールには、通常「稼働時間を集計する」機能が付随する。
実装するかどうかを、設計相談の段階で検討した。

判断の前提として、この2つの機能は求められる性質が正反対である。

| | 打刻（実行系） | 集計（分析系） |
|---|---|---|
| 頻度 | 1日2回・毎営業日 | 月1回程度 |
| 求めるもの | 1秒・100%決定的 | 柔軟さ・表現力 |
| 入力の揺らぎ | ない（ボタン2個） | 大きい（見たい切り口が毎回違う） |

**決定**

集計・レポート機能は本ツールに実装しない。
必要になった時点で、AI に Google カレンダーを参照させて集計させる。

**理由**

集計は「毎回違う切り口で見たい」性質を持ち、これは仕様を固定するコードが最も苦手とする領域である。
逆に LLM が最も得意とする領域でもある。

一方で打刻は決定性が命であり、LLM を挟むと劣化する。
つまり**同じドメインでも、実行系はコード、分析系は AI** という分担が最適解になる。
両方をコードで書けば、使われない集計機能の保守コストだけが残る。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| 月次集計画面を実装する | 月1回しか使わない機能のために、UI・集計ロジック・出力形式の保守が発生する。しかも「見たい切り口」は事前に確定できない |
| スプレッドシートに打刻を二重書き込みして集計 | 真実の源が二重化し、乖離したときの解決コストが高い（ADR-0006 と同じ理由） |
| 打刻自体も AI コネクタで行い、ツールを作らない | 決定性・応答速度・操作手数のいずれも打刻の要件を満たさない（PLAN.md §1） |

**影響**

- 良い影響: 実装・保守対象が打刻のみに絞られる。集計の切り口は後からいくらでも変えられる
- 悪い影響 / トレードオフ:
  - 集計のたびに AI へ依頼する手間がかかる。ワンクリックでは出てこない
  - 集計結果が実行のたびに揺らぎうる（決定的な数値が必要なら手計算での検算が要る）
  - AI 側に Google カレンダー連携が設定されていることが前提になる
- 再検討のトリガー: 毎月まったく同じ形式のレポートを出すことが確定した場合

---

<a id="adr-0011"></a>
### ADR-0011: 打刻に確認ダイアログを設けず、ボタン間隔と復旧手順で誤タップに対応する

- **ステータス**: 置換済み（ADR-0019 により置換）
- **日付**: 2026-08-05
- **区分**: 実装中（Phase 2）
- **関連**: ADR-0004, ADR-0006, PLAN.md §2.2 N-1/N-2, §7

**背景**

Phase 2 で UI を実装する段階になって、設計時に想定していなかった相互作用に気づいた。
既存の2つの決定が組み合わさることで、**誤タップのコストが単独の想定より高くなっている。**

- ADR-0004 により、打刻すると招待先2名に**即座に招待メールが飛ぶ**。
  誤りが自分のカレンダー内に留まらず、他人に届く。
- ADR-0006 により、同じ種別は1日1回しか打刻できない。
  「開始」を押すつもりで「終了」を押した場合、アプリ側からは訂正できない。

つまり誤タップは「押し直せば済む」ものではなく、他人への誤通知を伴い、
かつアプリ外での手作業復旧を要する。

一方で、確認ダイアログを挟むと打刻が3タップになり、
非機能要件 N-2（2タップ以内）を直接損なう。
1日2回・毎営業日という頻度では、この1タップの差は累積する。

**決定**

確認ダイアログ・長押し・取り消し機能のいずれも実装しない。
代わりに以下で対応する。

1. 「開始」「終了」ボタンの間に 32px の余白を取り、隣接タップを起きにくくする
2. ボタンの最小高を 88px とし、タップ位置の精度を要求しない
3. 押下直後にボタンを非活性化し、連続した誤操作を防ぐ
4. 誤タップ時の復旧手順（Google カレンダーから予定を削除する）を README に明記する

**理由**

打刻対象は2つしかなく、どちらも1日1回しか押せないため、
誤タップの発生確率は物理的な配置で十分に下げられる。
確率を下げる対策の方が、毎回のコスト（1タップ）を払う対策より費用対効果が高い。

取り消し機能を実装しない理由は別にある。イベントを削除すると招待先には
**キャンセル通知が飛ぶ**ため、アプリ内に取り消しボタンを置いても
「なかったこと」にはできない。誤操作の痕跡は必ず相手に届く。
アプリに機能を足しても解決しない問題であり、Google カレンダー本体の
削除操作に委ねる方が、動作が利用者にとって明確になる。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| 確認ダイアログを出す | N-2（2タップ以内）を損なう。1日2回・毎営業日の操作で確認を挟むコストが、誤タップの発生確率に見合わない |
| ボタンの長押しを要求する | 確認ダイアログよりは軽いが、操作方法の学習が必要になり、急いでいるときに反応しないという別のストレスを生む |
| 打刻後 N 秒間の取り消しボタンを出す | 削除時に招待先へキャンセル通知が飛ぶため「なかったこと」にできない。実装コストに対して解決する範囲が狭い |
| 押下から実際の作成まで数秒遅延させ、その間だけ取り消せる | N-1（3秒以内）と衝突し、打刻の完了が確定しない時間が生まれる。状態表示も複雑になる |

**影響**

- 良い影響: 打刻がホーム画面から2タップで完結する。UI とサーバーの状態遷移が単純に保たれる
- 悪い影響 / トレードオフ:
  - 誤タップは起きうる。起きた場合の復旧は Google カレンダーでの手作業になる
  - 復旧時、招待先2名にキャンセル通知が届く
  - 「なぜ確認がないのか」が UI からは読み取れないため、本 ADR と README がその根拠になる
- 再検討のトリガー: 実運用で誤タップが複数回発生した場合。
  その際はまず ADR-0004 を `sendInvites: false` に見直す方が、
  確認ダイアログを足すより誤タップの実害を小さくできる可能性が高い

---

## 実装中の決定（Phase 1 以降ここに追記）

実装に着手して以降に生じた判断は、上記のテンプレートに従い
`ADR-0011` から連番で本セクションに追記する。
特に以下に該当する事象は、設計時の想定が外れた記録として必ず残す。

- 実装してみたら設計時の前提が事実と異なっていた
- ライブラリ・API の制約により、当初の方針を変更した
- 性能・使用感の実測値が想定と乖離した

<a id="adr-0012"></a>
### ADR-0012: clasp をプロジェクトローカルに導入し、認証情報もリポジトリ内に閉じる

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 実装中
- **関連**: PLAN.md §3.3, PLAN.md §5 Phase 3, README.md セットアップ

**背景**

`src/` の4ファイルを GAS プロジェクトへ反映する必要がある。手段は clasp によるコマンドライン
push か、GAS エディタへの手貼りの2つ。README には当初 `npm install -g @google/clasp` を
記載していたが、開発端末のグローバル環境を汚したくないという制約が実装着手時に判明した。

グローバルインストールには次の実害がある。

- 端末に1バージョンしか置けず、他プロジェクトが別バージョンを要求すると衝突する
- リポジトリを消してもツールが残る。逆に、リポジトリだけを別端末に持っていっても動かない
- 必要な clasp のバージョンがリポジトリのどこにも記録されない

加えて `clasp login` は既定で `~/.clasprc.json`（ホーム直下）に OAuth トークンを書く。
これはリポジトリの外にある状態であり、削除し忘れが起きやすい。

**決定**

clasp をルートの `package.json` に `devDependencies` として宣言し、
`npm install` でプロジェクト内の `node_modules/` にのみ導入する。グローバルインストールはしない。

認証情報は clasp の `-A, --auth <file>` オプションでリポジトリ内の `.clasprc.json` に置く。
全ての clasp 実行は `--auth .clasprc.json` を含む npm scripts 経由で行い、素の
`npx clasp` を直接叩かない。`.clasprc.json` と `.clasp.json` は `.gitignore` 済みとする。

パッケージマネージャは npm を使う。bun / pnpm / mise には乗り換えない。

**理由**

バージョンが `package.json` と `package-lock.json` に固定され、リポジトリが自己完結する。
`rm -rf node_modules .clasprc.json` で導入前の状態に完全に戻せる。

npm を選ぶ理由は、キャッシュディレクトリ `~/.npm` が当該端末に既に存在するため。
bun / pnpm / mise はいずれもホーム直下に**新規の**キャッシュや shims を作るため、
「汚さない」という目的に対して逆効果になる。ダウンロードキャッシュ自体はどの手段でも必要であり、
既存のものを使い回すのが最小の副作用になる。

clasp v3.3.0 で `clasp create --type standalone --rootDir ./src` は v2 と同じ形で
使えることをヘルプで確認済みであり、README の既存記述を書き換える必要はない。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| `npm install -g @google/clasp`（README 初版の記述） | グローバル環境を汚す。バージョンがリポジトリに残らず、端末間で再現しない |
| `npx @google/clasp@3` の都度実行（インストールなし） | `node_modules/` は作られないが、結局 `~/.npm` に落ちるため汚染量は変わらない。一方でバージョン固定が効かず、実行のたびに解決コストがかかる |
| bun / `bunx` を使う | `~/.bun/install/cache` を新規に作る。npm キャッシュは既存なので、汚染箇所が1つ増えるだけになる |
| pnpm を使う | 同上。pnpm store を新規に作る |
| mise で clasp をツールとして管理する | shims とツール実体をホーム直下に新規作成する。ツール1つのために管理層を1段増やす割に得るものがない |
| clasp を使わず GAS エディタへ手貼りする | ツール導入はゼロになるが、4ファイルの貼り付けを変更のたびに手作業で行うことになり、`index.html` を HTML ファイルとして作る等の手順ミスも起きやすい |
| 認証情報を既定の `~/.clasprc.json` のままにする | ホーム直下にトークンが残り、リポジトリ削除時に消え残る。`--auth` で回避できる以上、受け入れる理由がない |

**影響**

- 良い影響:
  - グローバルの npm パッケージは `npm`・`corepack` のみのまま変わらない
  - clasp のバージョンがリポジトリに固定され、別端末でも `npm install` だけで再現する
  - `rm -rf node_modules .clasprc.json .clasp.json package-lock.json` で完全に撤去できる
- 悪い影響 / トレードオフ:
  - GAS 本体とは無関係な `package.json` / `node_modules/`（263パッケージ）がリポジトリ直下に同居し、
    このプロジェクトが Node.js アプリだという誤解を生みうる。PLAN.md §3.3 に注記して緩和する
  - `--auth` を付け忘れた素の `npx clasp` は別の認証状態を見に行き、未ログイン扱いになる。
    npm scripts 経由に統一することで運用上回避する
  - トークンがリポジトリ内に置かれるため、`.gitignore` の記述が保護の生命線になる
  - `~/.npm` キャッシュへの書き込みだけは避けられない
- 再検討のトリガー: clasp が v4 で `--auth` 相当の指定方法を変更した場合。
  または GAS 側の変更頻度が下がり、エディタ手貼りで足りると判断できた場合

<a id="adr-0013"></a>
### ADR-0013: OAuth スコープを `calendar` のみに限定し、マニフェストで明示宣言する

- **ステータス**: 置換済み（ADR-0014 により置換）
- **日付**: 2026-08-05
- **区分**: 実装中
- **関連**: ADR-0002, ADR-0007, ADR-0012, PLAN.md §3.2

**背景**

`clasp create-script` は新規プロジェクトのひな形マニフェストを `rootDir` に clone する。
この挙動により、2026-08-05 のセットアップ時に既存の `src/appsscript.json` が
GAS 既定値（`timeZone: America/New_York`、`webapp` ブロックなし、`oauthScopes` なし）で
上書きされ、そのまま push された。ADR-0002 と ADR-0007 の決定が一時的に失われた状態になった。

`timeZone` と `webapp` は ADR-0002 / ADR-0007 に値が記録されていたため復元できたが、
`oauthScopes` の元の内容はどのドキュメントにも記録がなく、復元できなかった。
現在マニフェストに入っているスコープ宣言は、コードが実際に使うサービスからの**再構成**であり、
上書き前の内容と一致する保証はない。

**決定**

`oauthScopes` に `https://www.googleapis.com/auth/calendar` のみを宣言する。

宣言の根拠はコード上の実使用に置く。現時点の使用サービスは以下の4つで、
OAuth スコープを要するのは `CalendarApp` だけである。

| サービス | 使用箇所 | 必要スコープ |
|---------|---------|-------------|
| `CalendarApp` | `Code.gs` `getCalendar()` / イベント作成・招待 | `auth/calendar` |
| `PropertiesService`（スクリプト プロパティ） | `Config.gs` | 不要 |
| `HtmlService` | `Code.gs` `doGet()` | 不要 |
| `LockService` | `Code.gs` `punch()` | 不要 |

スコープは GAS の自動推論に任せず、マニフェストに明示し続ける。

**理由**

明示宣言により、必要以上の権限が自動付与されることを防ぐ。本ツールは Drive・Gmail・
スプレッドシートのいずれにも触れないため、それらのスコープが混入する状態は避けたい。

`auth/calendar`（読み書き）を選ぶのは、イベント作成と招待者追加の両方が必要なため。
`calendar.readonly` では F-1〜F-3 を満たせない。

`oauthScopes` をマニフェストに書いておくこと自体が、今回のような上書き事故からの
復元可能性を担保する。ADR-0002 と ADR-0007 は値が文書に残っていたから復元できた。
同じ保険をスコープにも掛ける、というのが本レコードを起こす主目的である。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| `oauthScopes` を宣言せず GAS の自動推論に任せる | 推論結果がコード変更に応じて暗黙に広がる。何が承認されたかがリポジトリから読めず、今回のような上書き時に復元対象が存在しないことにも気づけない |
| `calendar.events` など、より細かいスコープを使う | `CalendarApp` は粗いスコープを要求するため、実際には `auth/calendar` に丸められる。細かく書いても権限は狭まらず、動作しないリスクだけが増える |
| `calendar.readonly` を併記する | `auth/calendar` に包含される。冗長 |
| `userinfo.email` を足す | 現在のコードは実行者のメールアドレスを参照していない。使わない権限を要求しない |

**影響**

- 良い影響:
  - 承認される権限がカレンダーだけに閉じ、リポジトリから確認できる
  - マニフェストが再び失われても、本レコードから3項目すべてを復元できる
- 悪い影響 / トレードオフ:
  - 上書き前の元スコープと一致する保証はない。**これは復元ではなく再構成である**
  - 新しい GAS サービスを使い始めたとき、`oauthScopes` の追記を忘れると実行時に権限エラーになる。
    自動推論なら起きなかった手間を引き受けている
  - スコープを変更した場合、既存の承認は無効になり再認可が必要（README トラブルシュート記載のとおり）
- 再検討のトリガー: `CalendarApp` 以外の OAuth スコープを要するサービスを使い始めたとき。
  その際は本レコードの表を更新するのではなく、新しいレコードを起こす

<a id="adr-0014"></a>
### ADR-0014: OAuth スコープを `calendar` のみに限定する（git 履歴で原本と一致を検証済み）

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 実装中
- **関連**: ADR-0013（本レコードにより置換）, ADR-0002, ADR-0007, ADR-0012, PLAN.md §3.2

**背景**

ADR-0013 は「`clasp create-script` に上書きされた `oauthScopes` の原本はどこにも記録が
残っておらず復元できない。現在の宣言はコードからの再構成であり、原本と一致する保証はない」
という前提で書かれた。

**この前提は誤りだった。** 本リポジトリは 2026-08-05 11:31 の
初期コミット `9359dc2` で git 管理下に入っており、上書きされる前の
`src/appsscript.json` がそこに保全されていた。上書きが起きたのは 11:35 の
`clasp create-script` 実行時で、コミットの4分後である。

`git show 9359dc2:src/appsscript.json` で取り出した原本と、ADR-0013 で再構成した
内容を突き合わせた結果、差分は JSON のキーの並び順のみで、**宣言内容は完全に一致**していた。

```
原本:     "oauthScopes": ["https://www.googleapis.com/auth/calendar"]
再構成:   "oauthScopes": ["https://www.googleapis.com/auth/calendar"]
```

現在は原本を `git checkout` で復元し、`push --force` 済み。GAS 側の実物を
別ディレクトリへ pull して初期コミットとの一致も確認した。

**決定**

`oauthScopes` に `https://www.googleapis.com/auth/calendar` のみを宣言する。
決定内容は ADR-0013 から変えない。根拠を「コードからの再構成」から
「**原本を git 履歴から復元し、コードの実使用とも一致することを検証済み**」に置き換える。

あわせて運用ルールを1つ追加する。**破壊的な可能性のある外部ツール
（`clasp create-script` など）を実行する前にコミットする。**

**理由**

決定そのものは検証によって裏付けられたため変更する理由がない。
一方で ADR-0013 の本文には「復元できなかった」「一致する保証はない」という
事実と異なる記述が残っており、これを 承認済み のまま放置すると後から読む人を誤導する
（運用ルール6）。本ファイルは過去レコードの本文を書き換えない方針（運用ルール3）のため、
ステータスを 置換済み に変更し、正しい根拠を持つ本レコードを起こす形で処理する。

記録すべき教訓は、スコープの中身ではなく手順にある。
**「復元不能」と判断する前に git 履歴を確認する。** 今回はそれを怠り、
原本が手元にあるにもかかわらず再構成という遠回りをした。
再構成が正解と一致したのは幸運であって、再現性のある手順ではない。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| ADR-0013 の本文を直接修正する | 運用ルール3（過去レコードの本文は書き換えない）に反する。誤った前提で判断した記録が残ること自体に価値がある |
| ADR-0013 を 承認済み のまま残し、本レコードを補足として追記する | 事実と異なる記述が 承認済み のまま残る。ステータスは有効性の表示であり、誤導を防ぐ責任がある |
| ADR-0013 を 棄却 にする | 棄却は「提案されたが採用されなかった」の意味。決定内容は採用され現に有効なので不適切 |
| 何も記録せず原本に戻すだけにする | 「create 前にコミットする」という手順上の教訓が失われる。同じ事故が再発する |

**影響**

- 良い影響:
  - スコープ宣言が推測ではなく検証済みの事実として確定した
  - `clasp create-script` 前のコミットが手順として明文化された（README にも記載）
  - ADR-0013 と本レコードの対比自体が、誤った前提で進めた判断の記録として残る
- 悪い影響 / トレードオフ:
  - 同一の決定に対してレコードが2つ並び、索引の見通しが悪くなる
  - git 履歴を保険とするため、コミットしていない変更は依然として保護されない。
    `clasp create-script` の上書き挙動そのものは変わらない
- 再検討のトリガー: `CalendarApp` 以外の OAuth スコープを要するサービスを使い始めたとき。
  その際は本レコードを更新せず、新しいレコードを起こす

<a id="adr-0015"></a>
### ADR-0015: セットアップ手順を AI Agent 向けに SETUP.md として文書化し、マニフェスト上書きを回避で防ぐ

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 実装中
- **関連**: ADR-0012, ADR-0014, PLAN.md §3.3

**背景**

本ツールを別のユーザーが自分の Google アカウントで動かす場合、GAS プロジェクトの作成から
デプロイまでの手順を踏む必要がある。この作業を AI Agent に任せたいという要求が出た。

README.md は人間の読者を想定しており、Agent に読ませるには次の点で不足がある。

- 人間にしか実行できない操作（OAuth 同意、GAS エディタでの関数実行、スクリプト プロパティ入力）と
  Agent が実行できる操作が区別されていない。Agent が代行を試みて破綻する
- 各ステップの成功判定が書かれていない。Agent は失敗に気づかず次へ進む
- ADR-0014 で記録した `clasp create-script` のマニフェスト上書き事故に対し、
  README の対策は「上書きされたら git から復元する」という**事後修復**だった。
  Agent が検証を飛ばせば、壊れたまま push される経路が残っている

特に3点目は実害が大きい。上書きされたマニフェストで push すると、打刻時刻が9時間ずれ、
かつ Web App が「自分のみアクセス」ではなくなる。**セキュリティ設定が静かに外れる。**

**決定**

`SETUP.md` を追加し、AI Agent 向けのセットアップ手順書とする。README.md は人間向けとして残す。

SETUP.md には以下を含める。

1. 🔴 STOP マーカーによる「人間にしか実行できないステップ」の明示
2. 各ステップ末尾の検証手順と期待結果
3. 禁止事項の明示（`npm install -g`、実アドレスのコード記載、検証を飛ばした push、
   OAuth 同意画面の代行操作、無断コミット）
4. 完了条件のチェックリスト

あわせて、マニフェスト上書きへの対策を**事後修復から事前回避に変更する**。
`npm run create` の `--rootDir` を `./src` から `./.gas-init`（捨てディレクトリ）に変更し、
ひな形マニフェストの clone 先を `src/` の外に逃がす。作成後に `.clasp.json` の
`rootDir` を `src` に書き換え、`.gas-init` を削除する。

push 前の検証ゲート（`timeZone` / `webapp` / `oauthScopes` の3点確認）は
回避策と併せて残す。git からの復元手順も fallback として維持する。

**理由**

`clasp create-script` がひな形を `--rootDir` の指定先へ clone することは、
2026-08-05 の事故で実地に観測済みである（`--rootDir ./src` を指定した結果、
`src/appsscript.json` が clone 先になった）。したがって指定先を `src/` の外に向ければ、
`src/` は構造上触られない。**壊れてから直すのではなく、壊れる経路自体を塞ぐ。**

`npm run create` のスクリプト自体を安全側に変更するのが要点である。
「危険なスクリプトを用意した上でドキュメントで使うなと書く」形にすると、
罠がリポジトリに残り続ける。Agent も人間も、手元のスクリプトを素直に実行して安全であるべき。

検証ゲートを残すのは、回避策が効かなかった場合（clasp の将来の挙動変更など）に
気づける層を1枚確保するため。回避と検証は排他ではない。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| README.md に「Agent 向け」セクションを追加するだけにする | 人間向けの散文と Agent 向けの手順が同じ文書に混在し、どちらにとっても読みにくくなる。Agent に「この節だけ読め」と指示する運用も、参照が README 全体に散るため成立しにくい |
| 事後修復（`git checkout` で戻す）のまま維持する | 検証を飛ばせば壊れたまま push される経路が残る。ADR-0014 の教訓は「コミットしておく」だったが、それは保険であって予防ではない。また git 管理下にないユーザーには保険すら効かない |
| `create` の後に `src/appsscript.json` を自動復元するスクリプトを書く | 復元スクリプト自体の正しさを検証する必要が生じる。clone 先を変えるだけで済む問題に、実行時ロジックを持ち込むのは過剰 |
| `.claspignore` で `appsscript.json` を push 対象から除外する | マニフェストこそ GAS に反映すべき対象。除外すると `timeZone` も `webapp` も永久に反映されない。問題を悪化させる |
| セットアップを自動化するシェルスクリプトを書く | 手順の大半（OAuth 同意、スクリプト プロパティ入力、デプロイ）が人間の対話操作であり、自動化できるのは一部にすぎない。中途半端な自動化は、どこまで済んだかの把握を難しくする |

**影響**

- 良い影響:
  - `npm run create` を素直に実行しても `src/` が壊れない。罠がリポジトリから消える
  - Agent が人間の担当範囲を代行しようとして破綻する経路が塞がれる
  - 検証ゲートにより、回避策が破れても push 前に気づける
- 悪い影響 / トレードオフ:
  - セットアップ手順が README.md と SETUP.md に二重化する。片方だけ更新すると乖離する。
    手順を変更するときは必ず両方を確認すること
  - `create` の直後に `.clasp.json` の `rootDir` 書き換えという手作業が1つ増える。
    clasp の標準的な使い方から外れるため、この ADR を読まないと理由がわからない
  - 捨てディレクトリ `.gas-init` が一時的に生成される（`.gitignore` 済み）。
    削除を忘れると紛らわしいファイルが残る
- 再検討のトリガー: clasp が `create-script` の clone 挙動を変更した場合
  （既存ファイルを上書きしなくなった場合を含む）。その際は回避策が不要になる可能性がある

<a id="adr-0016"></a>
### ADR-0016: 本番反映は既存デプロイのバージョン更新で行い、Web App URL を固定する

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 実装中
- **関連**: ADR-0002, ADR-0012, ADR-0015, PLAN.md §3.3, PLAN.md 非機能要件 N-2

**背景**

MVP 完成後、リポジトリを GitHub 管理下に置いた。この時点で「反映先」が3系統になった。

| 操作 | 反映先 | 本番への影響 |
|------|--------|-------------|
| `git push` | GitHub | なし |
| `npm run push` | GAS プロジェクト（＋ `@HEAD` デプロイ） | **なし** |
| バージョン作成＋デプロイ更新 | 本番 Web App URL | あり |

`clasp push` は名称に反して本番反映ではなく、ソースを GAS プロジェクトへ転送するだけである。
実際、本プロジェクトのデプロイは2つ存在し、本番はバージョン1に固定されている。

```
AKfycbws4jH...  @HEAD              テスト用。push した内容が即座に反映される
AKfycbyMVeb...  @1 - attendancem   本番。ホーム画面のアイコンが指す先
```

この分離を理解しないまま `npm run push` を「デプロイ」と認識すると、
**「直したのに本番が変わらない」**、あるいは逆に
**「確認前のコードが本番に出ている」**と誤認する。

加えて、GAS の「新しいデプロイ」は毎回**異なる URL を発行する**。
ホーム画面に追加したアイコンは URL を保持しているため、新規デプロイを作ると
アイコンが古いバージョンを指したまま取り残される。

**決定**

本番反映は、**既存デプロイのバージョンを更新する**方法で行う。
「新しいデプロイ」は作らない。これにより Web App URL を不変に保つ。

手順は `DEPLOY.md` に集約し、3系統の分離、リリース手順、ロールバック、
リリース前チェックリストを記載する。README.md の「コード変更を反映する」節は
要点のみを残して DEPLOY.md を参照させる。

GUI（GAS エディタの「デプロイを管理」）での操作を正の手順とする。
clasp による `create-version` + `redeploy` は代替手順として記載するが、
**本プロジェクトで未実行であることを明記**する。

**理由**

URL の固定は非機能要件 N-2（ホーム画面から2タップ）の前提である。
URL が変わればアイコンの作り直しが必要になり、作り直しを忘れれば
古いコードで打刻し続けることになる。打刻の記録が壊れるわけではないが、
不具合を修正しても本人に届かない状態が生まれる。

GUI を正とするのは、初回デプロイを GUI で実施済みであり動作が確認できているため。
clasp 版は `--force` の要否や既存デプロイ更新時の挙動を本プロジェクトで検証していない。
**検証していない手順を正として書くと、事故時に手順書自体が疑わしくなる。**
ADR-0014 で「再構成が正解と一致したのは幸運であって再現性のある手順ではない」と
記録したのと同じ理由で、未検証であることを明示する側に倒す。

`@HEAD` デプロイを残すのは、本番に影響を与えずに動作確認できる経路として有用なため。
GAS が既定で用意するものであり、削除する理由がない。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| README.md に節を足すだけで DEPLOY.md を作らない | README は既に「セットアップ・使い方・設定変更・トラブルシュート」を扱っており長い。リリース手順は参照頻度と緊急度が異なる（本番が壊れたときに開く文書）ため、独立させた方が辿り着きやすい |
| clasp による自動デプロイを正の手順とする | 未検証。また、デプロイのたびに `deploymentId` とバージョン番号を手で渡す必要があり、GUI のプルダウン選択より誤りやすい。検証後に正へ昇格させる余地は残す |
| GitHub Actions で push をトリガーに自動デプロイする | 打刻ツールの変更頻度は極めて低く、CI を維持するコストが見合わない。また CI から GAS を触るには OAuth 認証情報を GitHub Secrets に置く必要があり、ADR-0012 で「認証情報をリポジトリ内に閉じる」とした方針と衝突する |
| `@HEAD` デプロイの URL をホーム画面に登録し、常に最新を使う | `npm run push` した瞬間に本番が変わる。確認前のコードで打刻する事故が起きる。段階を設ける意味が失われる |
| 毎回「新しいデプロイ」を作り、その都度アイコンを作り直す | N-2 を損なう。アイコンの作り直しを忘れる経路が必ず残る |

**影響**

- 良い影響:
  - Web App URL が不変になり、ホーム画面のアイコンが永続的に有効
  - `push` と「デプロイ」の違いが文書として明示され、誤認による事故を防げる
  - `@HEAD` による本番非影響の確認経路が手順に組み込まれた
  - ロールバック手順（過去バージョンへのデプロイ切り替え）が明文化された
- 悪い影響 / トレードオフ:
  - リリース手順が README.md と DEPLOY.md に分かれ、参照先が増える。
    ADR-0015 で SETUP.md を分けたときと同じ二重化の問題を1つ抱える
  - 本番反映に GUI 操作が残るため、完全な自動化ができない。
    リリースのたびにブラウザを開く必要がある
  - clasp 版の手順が未検証のまま文書に載る。実行する人が最初の検証者になる
- 再検討のトリガー: clasp 版の手順を実行して動作を確認できたとき
  （その時点で正の手順を clasp 側に移すか判断する）。
  または変更頻度が上がり、GUI 操作が負担になったとき

---

<a id="adr-0017"></a>
### ADR-0017: 打刻種別を6種に拡張し、出社形態を独立したイベントタイトルで表現する

- **ステータス**: 置換済み（ADR-0023 により置換）
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0003, ADR-0006, ADR-0018, ADR-0020, PLAN.md §2.1 F-1〜F-3, §2.4

**背景**

MVP は「開始」「終了」の2種別で運用していた。実運用の要求として、
リアル出社とリモート出社を区別したい、休憩時間も記録したい、という2点が出た。

休憩の記録は PLAN.md §2.3 で明示的にスコープ外としていたため、
スコープ境界の変更にあたる。

表現方法には2案がある。

- A案: 6つの独立したイベントタイトルを持つ
- B案: タイトルは「開始」「終了」のまま維持し、リアル / リモートの区別は
  イベントの説明欄・色・場所などの属性に持たせる

**決定**

A案を採用する。イベントタイトルを次の6種とする。

| `type` | `title` |
|--------|---------|
| `officeIn` | `リアル出社` |
| `remoteIn` | `リモート出社` |
| `officeOut` | `リアル退社` |
| `remoteOut` | `リモート退社` |
| `breakIn` | `休憩開始` |
| `breakOut` | `休憩終了` |

改訂前の `開始` / `終了` は移行せず放置する。
新しいコードからは、履歴表示にも状態導出にも現れない。

**理由**

判定キーがタイトルの完全一致で済むため、既存の `findPunchEvent()` の
判定ロジックをそのまま使える。B案は説明欄の読み取りとパースが必要になり、
「カレンダーが唯一の記録先」であるがゆえに手で編集されうる欄に
判定を依存させることになる。

カレンダーを人が目視したとき、および AI に集計させるとき（ADR-0010）に、
タイトルだけで意味が完結することも大きい。B案では一覧表示で形態が見えない。

`type` と `title` を分けるのは既存の `PUNCH_TYPES` の構造を踏襲したものであり、
表示文言の変更が識別子に波及しないようにするため。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| B案（タイトル維持 + 属性で区別） | 判定が説明欄のパースに依存する。手編集で壊れる。カレンダー一覧で形態が見えない |
| 記号プレフィックスを付ける（`▶ リアル出社` 等） | 一覧での視認性は上がるが、タイトルに表示専用の情報が混ざる。判定が前方一致になり完全一致より弱くなる |
| 開始〜終了を1件のイベントに統合する（ADR-0003 の B案の再浮上） | ADR-0003 の再検討トリガー（集計要求の高まり）には該当していない。加えて休憩と同日複数ペアが入ることで、1件のイベントに畳む場合の状態管理はむしろ複雑になる。イベント ID の追跡が必須になり、ADR-0003 の却下理由が強まった |
| 改訂前の `開始` / `終了` を新タイトルへ移行する | 移行対象は数日分でしかなく、移行スクリプトを書いて検証するコストに見合わない。集計は AI に委ねる方針（ADR-0010）であり、表記ゆれは AI 側で吸収できる |

**影響**

- 良い影響:
  - 判定ロジックが変わらない。`PUNCH_TYPES` の要素を増やすだけで種別が増える
  - カレンダーを見ただけで勤務形態が分かる
- 悪い影響 / トレードオフ:
  - 改訂前の `開始` / `終了` は新しい画面から見えなくなる。過去分を遡って見るには
    Google カレンダー本体を直接見る必要がある
  - タイトル文字列が判定キーであるため、表示文言を変えたくなっても変えられない。
    「リアル出社」という言い回しを後から「オフィス出社」に変えると過去分と一致しなくなる
  - 種別が3倍になり、UI のボタン数も3倍になる（→ ADR-0019 が扱う）
- 再検討のトリガー: 種別をさらに増やしたくなった場合（外出・直行直帰など）。
  その時点でボタン数が UI の限界を超えるなら、タイトル + 属性の併用（B案）を
  改めて検討する

---

<a id="adr-0018"></a>
### ADR-0018: 同日複数回の打刻を許可し、重複禁止を状態遷移チェックに置き換える

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0006（本レコードにより置換）, ADR-0017, ADR-0019, PLAN.md §2.1 F-6/F-7, §2.4, §6 T-6〜T-10/T-14/T-15

**背景**

ADR-0006 は二重打刻を「LockService による直列化」と
「当日の同名イベントの存在チェック」の二段で防いでいた。
その影響欄には次のトレードオフを記載していた。

> 1日1回しか打刻できない。中抜け等で2回打刻したい運用には対応できない
> 再検討のトリガー: 1日に複数回の打刻が必要になった場合

実運用の要求として「1日中で複数回、出社・退社ペアがあってよい。
退社した後、同じ日でもまた出社できる」が出た。記載したトリガーに該当する。

同時に、存在チェックを外すと二重打刻に対して無防備になるため、
代替となる防壁が必要になる。

**決定**

1. **当日の同名イベントの存在チェックを廃止する。** 同日に同一種別が何度あってもよい
2. **LockService による排他は維持する。** 存在チェックとは目的が異なり、連打耐性は依然必要である
3. 存在チェックの代わりに、**勤務状態の遷移チェック**を置く。
   状態は `勤務外` / `勤務中` / `休憩中` の3つとし、遷移表は PLAN.md §2.4 に定義する
4. 状態は打刻イベント列から**毎回導出する**。状態そのものを永続化しない
5. 状態導出の窓は **前日 0:00 から現在まで**とする。画面に表示する履歴は当日分のみとする
6. **休憩中の退社は拒否する。** 先に `breakOut` を打たせる

**理由**

「同じ種別が1日に2回あってはならない」という制約は、実は本質ではなかった。
本質は「打刻列が勤務の実態として辻褄が合っていること」である。
状態遷移チェックはこの本質を直接表現しており、
かつ「勤務外 → 出社」を何度でも許すため、複数ペアと両立する。

存在チェックが防いでいた誤りのうち、
「勤務中にもう一度出社を押す」は遷移チェックが引き続き弾く。
弾けなくなるのは「退社後にもう一度出社を押す」だけであり、
これはまさに今回許可したい操作である。

LockService を残すのは、ADR-0006 の理由欄に書いたとおり、
ほぼ同時の2リクエストが競合しうるためである。存在チェックが無くなっても、
遷移チェックが同じ競合にさらされる（両方が「勤務外」と読んで両方が作成に進む）。
むしろ排他の必要性は変わっていない。**存在チェックの廃止に引きずられて
LockService まで外すと、T-12（連打耐性）が落ちる。**

状態を永続化しないのは、ADR-0006 で「Script Properties に打刻状態を保存する案」を
「真実の源が二重化する」として却下したのと同じ理由である。
カレンダー側を手で修正したとき、ツールの認識が自動的に追従する性質を保つ。

状態導出の窓を前日 0:00 まで広げるのは、当日 0:00 で切ると
日を跨いだ勤務の退社が「勤務外からの退社」として拒否されるためである。
MVP は1日1ペア前提だったためこの問題が表面化しなかったが、
複数ペアを許す以上、夜勤・残業の跨ぎは現実的な経路になる。

休憩中の退社を拒否するのは、休憩が閉じないまま退社すると
招待先が実働時間を計算できなくなるためである。
「押したのに拒否される」というコストを払ってでも、記録の完全性を優先する。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| 存在チェックを「N 件まで」に緩める | 上限の根拠が無い。中抜けが3回あった日に4件目が弾かれる理由を説明できない |
| 遷移チェックを設けず、どんな順序も受け付ける | 誤タップが完全に素通りする。ADR-0019 で誤タップ対策の主軸を遷移チェックに置いているため、これを外すと防壁が連打しきい値だけになる |
| 状態を Script Properties に保存する | ADR-0006 と同じ理由で却下。カレンダーを手修正したときに乖離する |
| 状態導出の窓を当日 0:00 のままにする | 日跨ぎ勤務の退社が拒否される。実運用で詰む |
| 状態導出の窓を「直近 24 時間」にする | 前日 0:00 起点より窓が狭く、前日朝の出社を拾えない場合がある。また窓の起点が呼び出し時刻に依存し、同じ状況でも時刻によって結果が変わる |
| 休憩中の退社を許可し、休憩を自動的に閉じる | 打刻していない `休憩終了` をツールが勝手に作ることになる。カレンダーが唯一の記録先である以上、利用者が打っていない記録を生成すべきでない |
| 休憩中の退社を警告付きで許可する | 警告は無視される前提で設計すべきであり、拒否と実質的に変わらないか、記録が壊れるかのどちらかになる |

**影響**

- 良い影響:
  - 中抜け・分割勤務が記録できる
  - 「同名イベントを別目的で手動作成すると打刻済みと誤判定される」という
    ADR-0006 の欠点が解消する
  - 日跨ぎ勤務の退社が受理される
- 悪い影響 / トレードオフ:
  - 状態導出のためにカレンダー読み込みが1日分から2日分に増える。
    応答時間は延びるが N-1（3秒以内）には収まる見込み。実測は #5 で確認する
  - 遷移表がサーバー・PLAN.md・テスト観点の3箇所に現れる。
    クライアントには持たせない設計（`allowed` をサーバーが返す）にして、
    重複を1箇所減らしている
  - 休憩を閉じ忘れて退社しようとすると打刻できず、その場で戸惑う
  - 「勤務外」からいきなり退社を押せないため、出社の打刻を忘れた日は
    退社も打てない。カレンダー本体で出社を手作成してから退社を打つ必要がある
  - 前日 0:00 起点の窓は、2日以上連続した勤務には対応しない
- 再検討のトリガー:
  - 休憩の閉じ忘れによる拒否が繰り返し起きた場合（自動クローズの是非を再検討する）
  - 出社の打刻忘れが頻発した場合（勤務外からの退社を許すかを再検討する）
  - 2日以上跨ぐ勤務が発生した場合（窓の起点を再検討する）

---

<a id="adr-0019"></a>
### ADR-0019: 誤タップ対策の主軸を、ボタンの物理的配置から状態遷移チェックと連打しきい値に移す

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0011（本レコードにより置換）, ADR-0017, ADR-0018, ADR-0020, PLAN.md §2.1 F-8, §2.2 N-2

**背景**

ADR-0011 は誤タップ対策として、確認ダイアログを設けない代わりに
ボタンの物理的な配置（最小高 88px・間隔 32px）と押下直後の非活性化を採用した。
その理由欄には次のように書いていた。

> 打刻対象は2つしかなく、どちらも1日1回しか押せないため、
> 誤タップの発生確率は物理的な配置で十分に下げられる。

この理由の前提が、ADR-0017 と ADR-0018 の両方によって崩れた。

- ADR-0017 により**ボタンは2つから6つになる**。物理的配置による確率低下は、
  88px × 6 + 32px × 5 = 688px となり iPhone の1画面に収まらないため、
  そのままの寸法では成立しない
- ADR-0018 により**同じ種別を1日に何度でも押せる**。
  「1日1回しか押せない」という確率の前提が消えた

一方、ADR-0011 が確認ダイアログを却下した理由（N-2 の2タップ以内を損なう）と、
取り消し機能を却下した理由（削除すると招待先にキャンセル通知が飛ぶ）は、
いずれも今回の変更で無効化されていない。

**決定**

確認ダイアログ・長押し・取り消し機能を実装しない方針は維持する。
誤タップ対策の主軸を、次の2つに移す。

1. **状態遷移チェック**（ADR-0018）。現在の状態から遷移できない打刻を拒否する
2. **連打しきい値**。直前の打刻から `MIN_PUNCH_INTERVAL_MS`（既定 60 秒）未満の打刻を、
   **種別を問わず**拒否する

ボタンの物理的な寸法・配置は、6ボタンを前提に
[#4](https://github.com/diwamotoG/attendancem/issues/4) で改めて決定し、別の ADR に記録する。
本レコードは寸法を規定しない。

**理由**

誤タップの経路を分類すると次の2つになる。

- **順序違反型**: 押すはずのないボタンを押す（勤務中に出社、休憩中に退社など）。
  状態遷移チェックがすべて弾く
- **隣接誤タップ型**: 押すべきボタンの隣を押す（リアル退社のつもりでリモート退社）。
  遷移チェックは通ってしまう。ここを連打しきい値が受け持つ

しきい値を種別によらず一律に適用するのは、隣接誤タップ型が
「遷移として正しい別種別」で起きるためである。同一種別だけを対象にすると、
最も防ぎたいケースを取りこぼす。

60 秒という値は、正当な操作で 60 秒未満の間隔が生じる場面が
実質的に無いことから決めた。最短でも「休憩開始 → 休憩終了」であり、
1分未満の休憩は打刻する意味がない。

ADR-0011 の「取り消しを実装しない」判断は今回も維持する。
削除時に招待先へキャンセル通知が飛ぶという事実は変わっておらず、
アプリ内に取り消しを置いても「なかったこと」にはできない。
ただし ADR-0020 により休憩はメールを送らなくなったため、
**休憩の誤タップに限っては実害が小さくなっている。**

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| ADR-0011 の寸法をそのまま6ボタンに適用する | 688px となり iPhone の1画面に収まらない。1タップ目までの視認性が落ち、N-2 の趣旨を損なう |
| 状態遷移チェックのみ（しきい値なし） | 隣接誤タップ型（リアル退社 → リモート退社の押し間違い）が素通りする。遷移としては正しいため弾けない |
| 連打しきい値のみ（遷移チェックなし） | 順序違反型が素通りする。「勤務外なのに退社」のような明らかに不正な記録が残る |
| しきい値を同一種別だけに適用する | 最も防ぎたい隣接誤タップ型が別種別で起きるため取りこぼす |
| 6ボタンになったことを機に確認ダイアログを導入する | N-2（2タップ以内）を損なうという ADR-0011 の却下理由は今も有効。打刻頻度はむしろ増えており、1タップの追加コストは MVP 時点より重い |

**影響**

- 良い影響:
  - 誤タップ対策が UI のレイアウトから独立し、サーバー側で担保される。
    #4 でどのレイアウトを選んでも防壁の強度が変わらない
  - 順序違反型の誤タップは、招待メールが飛ぶ前に弾かれる。
    ADR-0011 では作成された後に手で復旧するしかなかった
- 悪い影響 / トレードオフ:
  - 正当な操作でも 60 秒以内の連続打刻は拒否される。
    「退社してすぐ再出社」のような操作は 1 分待つ必要がある
  - しきい値による拒否は、利用者からは「なぜか打刻できない」と見える。
    エラーメッセージに直前の打刻時刻と種別を含めて、理由を明示する必要がある
  - 隣接誤タップ型のうち 60 秒以上経ってから起きたものは、依然として防げない。
    復旧は Google カレンダーでの手作業になる（README の既存手順）
  - ボタンの寸法という具体的な保護策が本レコードから抜けたため、
    #4 の決定が出るまで UI 側の誤タップ耐性は未規定の状態になる
- 再検討のトリガー:
  - 60 秒のしきい値が正当な操作を妨げた場合（値を下げるか、種別ごとに分ける）
  - 隣接誤タップが実運用で繰り返し起きた場合
    （リアル / リモートの選択を2段階に分けることを検討する）

---

<a id="adr-0020"></a>
### ADR-0020: 招待メールの送信可否を打刻種別ごとに分け、休憩は招待のみで送信しない

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0004（本レコードにより置換）, ADR-0017, ADR-0018, PLAN.md §2.1 F-4/F-5, §4.3, §6 T-5, §7

**背景**

ADR-0004 は `sendInvites: true` を全打刻に一律で適用し、
年間約500通（1日2件 × 250営業日）という通数を受け入れる決定をしていた。
その再検討トリガーには次のように書いていた。

> 招待先から通知量について苦情や要望があった場合。
> その際は設定フラグを `false` に変更し、本 ADR を置換するレコードを起こす

今回のトリガーは苦情ではないが、通数を増やす変更が2つ同時に入る。

- ADR-0017 により休憩打刻が追加される。1日1回の休憩で年間 +250 通
- ADR-0018 により同日複数ペアが可能になる。中抜けした日はさらに増える

ADR-0004 の決定を維持すると、年間通数が 750 通を超える。
ADR-0004 が「悪い影響」として挙げた
「招待先のメールボックスが打刻通知で埋まり、重要な連絡が埋没するおそれ」が
より深刻になる。

ADR-0004 の背景欄に記録されていた事実が、ここで効いてくる。

> `sendInvites: false` — メールは送信されないが、招待者のカレンダーには
> 予定として表示される（個人 Gmail の既定設定では招待は自動的にカレンダーに追加されるため）

つまり**メール送信と情報共有は分離できる。**

**決定**

`SEND_INVITES` という単一のフラグを廃止し、
送信可否を `PUNCH_TYPES` の種別ごとの属性に移す。

| 種別 | 招待者の追加 | 招待メール |
|------|------------|-----------|
| `officeIn` / `remoteIn` / `officeOut` / `remoteOut` | する | **送る** |
| `breakIn` / `breakOut` | する | **送らない** |

休憩も招待者には追加する。招待先のカレンダーには表示されるため、
実働時間から休憩を差し引く計算は引き続き可能である。

**理由**

出社・退社は「今日の稼働が始まった / 終わった」という、
招待先が能動的に知る価値のあるイベントである。ADR-0004 が
通知の確実性を優先した判断は、この2種別については今も妥当である。

休憩は性質が異なる。招待先が休憩の開始を即座に知る必要は薄く、
必要になるのは後から実働時間を計算するときである。
その用途はカレンダー上の表示だけで満たせる。

結果として、年間通数は ADR-0004 の想定（約500通）から
実質的に増えない。増えるのは中抜けした日の出社・退社の分だけであり、
これは「その日に実際に稼働が分割された」という共有すべき事実に対応している。

フラグを種別ごとの属性に移すのは、ADR-0004 が
「フラグを1箇所に集約し、コード全体を横断せずに切り替えられる構造とする」
と定めた設計上の保険を、粒度を細かくして引き継ぐものである。
集約先が `Config.gs` の1箇所である点は変わらない。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| ADR-0004 を維持し、休憩にもメールを送る | 年間通数が 750 通を超える。ADR-0004 自身が挙げた「重要な連絡が埋没する」リスクが現実的な水準になる |
| 休憩には招待者を追加しない（自分だけ） | メール通数は同じく増えないが、招待先が実働時間から休憩を差し引けなくなる。共有の目的を一部損なう |
| 全種別を `sendInvites: false` にする | メールは 0 通になるが、招待先がカレンダーを日常的に見ない運用では共有が成立しない。ADR-0004 で既に却下した理由がそのまま当てはまる |
| 1日の終わりに打刻をまとめて1通で通知する | 通数は最小になるが、リアルタイム性が失われる。加えて集計・送信のトリガー実装が必要になり、ADR-0010（集計は実装しない）と衝突する |

**影響**

- 良い影響:
  - 休憩打刻を追加しても、招待先のメール通数が増えない
  - 休憩の誤タップの実害が小さい。メールが飛ばないため、
    カレンダーから静かに削除すれば済む（キャンセル通知は飛ぶ点に注意）
  - 送信可否が種別ごとに独立したため、後から個別に切り替えられる
- 悪い影響 / トレードオフ:
  - 招待先が休憩を知るにはカレンダーを見る必要がある。
    メールだけを見ている相手には休憩が見えない
  - 種別ごとにフラグが分かれたため、「全部止める」操作が
    単一フラグの変更から6箇所の変更になった
  - **メールが飛ばないことの検証が、カレンダーだけを見ても行えない。**
    招待先の受信箱を実際に確認する必要がある（PLAN.md T-5 にこの注意を明記した）
- 再検討のトリガー:
  - 招待先から「休憩もメールで知りたい」という要望が出た場合
  - 出社・退社のメール通数についても苦情が出た場合
    （その際は出社・退社側も `false` に倒す。本 ADR を置換するレコードを起こす）

---

<a id="adr-0021"></a>
### ADR-0021: 勤務状態の遷移表だけを純粋関数に切り出し、GAS 上の簡易テストで網羅する

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 設計時
- **関連**: ADR-0018, ADR-0010, PLAN.md §6.1

**背景**

MVP では PLAN.md §6 に「自動テスト基盤は持たない（GAS の小規模ツールのため、
手動検証で担保する）」と定め、T-1〜T-10 の手動検証だけで品質を担保していた。
打刻ロジックが「当日に同名イベントがあるか」の一点だったため、
観点を目視で数え上げることができた。

ADR-0018 により、判定が **3状態 × 6種別の遷移表**になった。
組み合わせは 18 通りあり、うち許可されるのは 7 通りに過ぎない。
手動検証で 18 通りを毎回なぞるのは現実的でなく、
実際 PLAN.md §6.2 の手動観点は遷移違反を T-8 / T-9 / T-10 の
3 ケースしかカバーしていない。**残る 8 通りの拒否ケースは未検証のまま残る。**

一方で、テストフレームワークの導入には別の問題がある。
GAS には標準のテストランナーが無く、clasp 経由で Node 側にロジックを
切り出す構成にすると、ADR-0012 で「開発端末で clasp を動かすためだけ」と
位置づけた `node_modules` の役割が広がる。

**決定**

1. 遷移の判定を**副作用のない2つの関数**に切り出す。
   いずれも `CalendarApp` / `PropertiesService` / `new Date()` に触れない
   - `getAllowedTypes(state)` — 状態から許可される `type` の配列を返す
   - `deriveState(types)` — 打刻 `type` の配列から到達状態を返す
2. `testTransitions()` を実装する。GAS エディタから引数なしで実行でき、
   **18 通りすべて**と代表的な打刻列を検証して失敗件数を `Logger` に出す
3. 外部のテストフレームワーク・CI は導入しない
4. 自動化の対象は上記の純粋ロジックに限る。
   カレンダー I/O・招待メール・UI・認証は従来どおり手動検証（PLAN.md §6.2）とする

**理由**

自動化すべきかどうかの線引きを「重要かどうか」ではなく
**「副作用なしに検証できるか」**に置いた。遷移表はこの条件を満たす唯一の部分であり、
かつ組み合わせ爆発によって手動検証が最も破綻しやすい部分でもある。
費用対効果が明確に高い。

逆にカレンダー連携やメール送信は、モックを書けば「モックが正しいこと」を
別途保証する必要が生じ、GAS の実挙動との乖離を検証できない。
ADR-0004 系の「メールが実際に届くか」は受信箱を見る以外に確かめようがなく
（PLAN.md T-5）、自動化しても手動検証を減らせない。

`testTransitions()` を GAS エディタ実行の関数とするのは、
`checkConfig()` / `testPunchStart()` という既存の動作確認用ラッパーと
同じ流儀に揃えるためである。新しい実行手段を増やさない。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| 従来どおり手動検証のみ | 18 通りのうち手動観点がカバーするのは 3 通り。遷移表の書き間違いが本番まで残る |
| GasT / QUnitGS2 などの GAS 用テストライブラリを導入する | 依存が増え、ライブラリ側の GAS 仕様追従に運用が縛られる。検証したいのは純粋関数 2 つであり、アサーションは自前で十分足りる |
| ロジックを Node 側に切り出し、Jest 等で実行する | ADR-0012 で `node_modules` を「clasp を動かすためだけ」と限定した位置づけが崩れる。また GAS 側とロジックが二重化するか、ビルド工程が必要になる |
| カレンダー I/O もモックして自動化する | モック自体の正しさを保証できず、GAS の実挙動との乖離を検出できない。手動検証を減らせないまま保守対象だけ増える |
| GitHub Actions で CI を回す | `testTransitions()` は GAS ランタイム上で実行する前提であり、CI から起動する経路を作ると認証情報を CI に置くことになる。ADR-0012 で認証情報をリポジトリ内に閉じた方針と衝突する |

**影響**

- 良い影響:
  - 遷移表の書き間違いが、カレンダーに1件も書き込まずに検出できる
  - `#4` の UI 実装が `allowed` を信頼してよい根拠ができる
  - 遷移表を将来変更したとき、回帰の検出が自動になる
- 悪い影響 / トレードオフ:
  - **`punch()` の実装が制約を受ける。** 遷移判定を `punch()` の中に直接書けず、
    純粋関数として分離し続ける必要がある。この制約が守られなくなると
    テストは通るのに本番が壊れる状態になりうる
  - 遷移表が §2.4（仕様）・`PUNCH_TYPES`（実装）・`testTransitions()`（テスト）の
    3 箇所に現れる。テストは仕様の写しであり、両方を直さないと意味を成さない
  - 自動テストがあることで、手動検証が省略されやすくなる。
    §6.1 が担保するのは遷移表だけであり、それ以外は依然 T-1〜T-20 が必要である
  - GAS エディタを開いて手で実行する必要があり、コミット時に自動では走らない
- 再検討のトリガー:
  - 遷移以外にも純粋ロジックが増えた場合（時刻計算・集計など）
  - `testTransitions()` の手動実行が忘れられ、遷移の回帰が本番に出た場合
    （その時点で CI からの起動経路を改めて検討する）

---

<a id="adr-0022"></a>
### ADR-0022: 6つの打刻ボタンを2列3行のグリッドに配置し、88px の寸法を維持する

- **ステータス**: 承認済み
- **日付**: 2026-08-05
- **区分**: 実装中
- **関連**: ADR-0019（本レコードは置換ではなく補完）, ADR-0011, ADR-0017, PLAN.md §2.1 F-10, §2.2 N-2/N-5, §3.2, §7

**背景**

ADR-0011 は誤タップ対策として、ボタンに min-height 88px・間隔 32px を課していた。
ADR-0019 はその主軸をサーバー側（状態遷移チェックと連打しきい値）に移した上で、
寸法については次のように書き、決定を持ち越していた。

> ボタンの物理的な寸法・配置は、6ボタンを前提に #4 で改めて決定し、
> 別の ADR に記録する。本レコードは寸法を規定しない。

その結果、ADR-0019 の「悪い影響」に次の状態が残っていた。

> ボタンの寸法という具体的な保護策が本レコードから抜けたため、
> #4 の決定が出るまで UI 側の誤タップ耐性は未規定の状態になる

本レコードはこの穴を埋めるものであり、**ADR-0011 を置換しない**
（ADR-0011 は既に ADR-0019 により置換済みである）。
ADR-0019 も置換しない。ADR-0019 が意図的に空けた寸法・配置の欄を埋める補完にあたる。

制約は次のとおり。6ボタンを1列に縦積みし ADR-0011 の寸法をそのまま適用すると
88×6 + 32×5 = 688px となり、iPhone の1画面（N-5）に収まらない。

**決定**

1. 6ボタンを **2列3行のグリッド**に配置する。行は上から
   出社行（リアル / リモート）、退社行（リアル / リモート）、休憩行（開始 / 終了）とする
2. **min-height 88px を維持する。** 行間 32px も維持する。列間は 12px とする
3. 全6ボタンを常時表示する。押せないボタンは **非活性にする**（F-10 の維持）。
   状態によって表示・非表示を切り替えない
4. 活性の判定は `Status.allowed` に含まれるかどうかだけを見る。
   遷移表をクライアントに持たせない（PLAN.md §4.1）

高さは 88×3 + 32×2 = 328px となり、日付・履歴カード・招待先表示を含めても
iPhone の1画面に収まる。

**理由**

「全ボタンを常に同じ位置に置く」ことを、寸法の維持と同格の要件として扱った。
打刻は毎営業日に数回、ほぼ無意識に行う操作であり、
**押す位置が状態によって動くこと自体が誤タップの原因になる**と判断した。
allowed のボタンだけを表示する案は寸法を完全に維持できるが、
直前まで別の種別があった位置に別のボタンが来るため、この要件を満たさない。

グリッドにすることで生じる押し間違いは、
ADR-0019 が分類した2つの経路に照らして次のように整理できる。

- **行をまたぐ押し間違い**（出社のつもりで退社、退社のつもりで休憩）は
  ADR-0019 の「順序違反型」にあたり、状態遷移チェックがすべて弾く。
  行間 32px を維持したのは、この距離を縮める理由が無いためである
- **同じ行内の押し間違い**（リアルのつもりでリモート）は「隣接誤タップ型」であり、
  遷移としては正しいため弾けない。ADR-0019 が連打しきい値に担わせた領域そのものであり、
  グリッド化によって新たに生まれたリスクではなく、**位置が変わっただけ**である

列間を 32px ではなく 12px にしたのは、iPhone の幅（390px）で
1ボタンあたりの幅を確保するためである。列間 32px では1ボタン 161px となり、
「リモート出社」の6文字が折り返すか字詰まりする。
隣接誤タップ型の防壁が連打しきい値である以上、
列間の距離を稼ぐことによる寄与は小さく、可読性を優先した。

休憩行を最下段に置いたのは、出社・退社に比べて打刻頻度が低く、
かつ ADR-0020 により誤タップの実害（招待メール）が無い唯一の行だからである。
親指の可動域の中心から最も遠い位置を、最も実害の小さい種別に割り当てる。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| `allowed` のボタンだけを表示する（最大3つ） | 寸法を完全に維持でき誤タップ対象も減るが、状態ごとにボタンの位置が動く。指の記憶が効かず、直前まで別種別があった位置に別のボタンが来る。F-10 の「非活性にする」を「非表示にする」に変える PLAN 改訂も必要になる |
| 6つを縦1列に積み、min-height を 72px・間隔 16px に縮める | 位置は固定できるが、ADR-0011 が定めた寸法を明示的に緩めることになる。1画面に収めるために 512px を要し、履歴カードを足すと窮屈になる |
| 6つを縦1列に積み、88px/32px のまま縦スクロールを許容する | 688px となり、退社・休憩が初期表示で見えない。N-2（2タップ以内）の趣旨である「開いてすぐ押せる」を損なう |
| 出社/退社を選んでから形態を選ぶ2段階選択 | タップ数が2から3に増え、N-2 に正面から抵触する。1タップ・1秒という PLAN.md §1 の最重要要件に反する |
| 休憩ボタンだけサイズを下げる | ADR-0011 の保護範囲を種別ごとに変える判断になり、根拠を「誤タップの実害が小さい」に置くことになる。実害の大小は ADR-0020 の送信可否に依存しており、そちらを変えると寸法の根拠も崩れる |
| 列間も 32px にする | 1ボタン 161px となり「リモート出社」が折り返す。隣接誤タップの防壁は連打しきい値であり、列間を広げる寄与は小さい |

**影響**

- 良い影響:
  - 6種別すべてが常に同じ位置に見える。押す位置が状態で動かない
  - ADR-0011 の 88px と F-10 の「非活性にする」を、どちらも変更せずに済む
  - 1画面に収まり、開いてすぐ押せる（N-2）
  - 非活性のボタンが「今は押せない」ことを示すため、
    状態遷移の拒否をエラーとして体験する前に視覚的に伝えられる
- 悪い影響 / トレードオフ:
  - 同じ行のリアル↔リモートが横に隣接する。押し間違いは遷移として正しく、
    連打しきい値をすり抜けた場合（60秒以上経過後）は防げない。
    復旧は Google カレンダーでの手作業になる
  - ボタン1つあたりの幅が 171px 程度になり、MVP の全幅ボタンより小さくなる
  - 6ボタンぶんのラベルが並ぶため、情報量が増える。
    履歴カードを簡潔に保たないと1画面に収まらない
  - 横並びは横スクロールの発生源になりうる。T-19 で必ず確認すること
- 再検討のトリガー:
  - リアル↔リモートの押し間違いが実運用で繰り返し起きた場合
    （ADR-0019 が既に挙げているトリガーと同一。2段階選択を再検討する）
  - iPhone 以外の画面幅で列が破綻した場合
  - 打刻種別がさらに増えた場合（3列化ではなく、種別の整理から検討する）

---

<a id="adr-0023"></a>
### ADR-0023: 打刻タイトルを `{種別名}_{氏名}` とし、種別判定を最初の `_` の前の完全一致に置き換える

- **ステータス**: 承認済み
- **日付**: 2026-08-06
- **区分**: 設計時
- **関連**: ADR-0017（本レコードにより置換）, ADR-0024, PLAN.md §2.1 F-13, §2.4, §4.1

**背景**

打刻イベントは招待先2名の共通カレンダーに表示される。
現状のタイトルは種別名だけであり、**誰の打刻かがタイトルから分からない**。
[#9](https://github.com/diwamotoG/attendancem/issues/9)（複数ユーザー対応）が入ると
同じカレンダーに複数人の打刻が並ぶため、判別できないことが実害になる。

ADR-0017 は判定キーをタイトルの**完全一致**と決めており、
記号プレフィックス案を「判定が前方一致になり完全一致より弱くなる」として却下している。
本件はその却下した形をまさに要求する。

**決定**

1. カレンダー上のタイトルを `{種別名}_{氏名}` とする。区切り文字は `_`（U+005F）
2. 種別判定は、タイトルを**最初の `_` で1回だけ分割**し、
   前半が種別名と**完全一致**するかで行う。**前方一致では判定しない**
3. `_` を含まないタイトル、および `_` より後ろが空のタイトルは打刻ではない
4. **種別名に `_` を含めてはならない。** 氏名側の `_` は許容する
5. 旧形式（氏名なし）は打刻として認識しない。移行もしない
6. `Status.punches[].title` はカレンダー上のタイトルをそのまま返し、UI もそのまま表示する

**理由**

ADR-0017 の却下理由「前方一致は完全一致より弱い」は、
**前方一致そのものを採らないことで回避できる**。
分割してから完全一致するため、種別部分の判定強度は ADR-0017 と変わらない。
`リアル出社について_議事録` のような予定は前半が一致せず弾かれ、
これは前方一致（`リアル出社_` で始まるか）では表現できない厳しさである。

分割位置を最初の `_` に固定するのは、氏名側に `_` が入っても後半に残るだけで
判定に影響しないためである。氏名は利用者が入力する自由文字列であり、
区切り文字を含まない保証を利用者に課したくない。

旧形式を切り捨てるのは、ADR-0017 が改訂前の `開始` / `終了` に対して
「移行せず放置」を選んだのと同じ判断に倒したものである。
読み取りのみ互換にする案は移行時の断絶を消せるが、
判定規則が「`_` があれば分割、なければ全体」と2本になる。
移行という一時的な事情のために恒久的な規則を分岐させるより、
規則が1本であることを優先した。

`Status.punches[].title` に氏名を含めるのは、
**保存される内容が画面でそのまま確認できる**状態を保つためである。
氏名は利用者の自己申告であり（ADR-0024）、入力ミスに気づける経路が要る。
種別名だけを返すと、誤った氏名がカレンダー上に増え続けても画面からは分からない。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| 前方一致（`種別名 + '_'` で始まるか） | 検出範囲はほぼ同じだが、判定規則が前方一致になり ADR-0017 の却下理由をそのまま踏む。`リアル出社_` で始まる無関係な予定を拾う危険が新たに生まれる。分割方式なら種別部分は完全一致のままで、この危険が増えない |
| 区切り文字を空白にする | 種別名にも氏名にも空白が入りうる（「山田 太郎」）。境界が曖昧になり、分割位置を決められない。`_` は打刻タイトルの語彙に現れず、氏名にも通常は現れない |
| 氏名を説明欄・場所に持たせる | ADR-0017 の B案と同じ却下理由。加えて「共通カレンダーの一覧でタイトルだけで判別する」という本件の目的をそもそも満たさない |
| 旧形式を読み取りのみ互換にする | 切り替え時に勤務中でも退社が打てる利点はあるが、判定規則が2本になる。ADR-0017 の前例（移行せず放置）とも一貫しない |
| 氏名を先頭に置く（`{氏名}_{種別名}`） | 分割の前半が自由文字列になり、完全一致で照合できるのは後半だけになる。氏名に `_` が入ると分割位置が氏名の内部に来て壊れる |

**影響**

- 良い影響:
  - 共通カレンダー上で誰の打刻かがタイトルだけで分かる
  - [#9](https://github.com/diwamotoG/attendancem/issues/9) でそのまま機能する。複数ユーザー対応の側で追加の設計が要らない
  - 種別部分の判定強度が ADR-0017 から落ちない
- 悪い影響 / トレードオフ:
  - **反映時点で勤務中だと退社が打てなくなる。** 旧形式の出社が状態導出から消え、
    状態が `offDuty` に戻るため。勤務外の状態で反映する運用制約が生まれる（DEPLOY.md）
  - 旧形式の打刻が履歴からも状態導出からも消える。過去分は Google カレンダー本体で見る
  - 種別名に `_` を使えないという制約が新たに加わる
  - `Status.punches[].title` に氏名が入るため、自分の画面で毎行に自分の氏名が並ぶ
  - 氏名を変えても過去のイベントは書き換えないため、カレンダー上に新旧の氏名が混在する
  - ADR-0017 が挙げた「タイトル文字列が判定キーであるため表示文言を変えられない」は
    種別名について引き続き有効である
- 再検討のトリガー:
  - 種別名に `_` を含めたくなった場合
  - 氏名以外の情報もタイトルに載せたくなった場合（区切り規則の再設計が要る）

---

<a id="adr-0024"></a>
### ADR-0024: 氏名を画面からの入力で受け取り、User Properties に保存する

- **ステータス**: 承認済み
- **日付**: 2026-08-06
- **区分**: 設計時
- **関連**: ADR-0009, ADR-0014, ADR-0023, PLAN.md §2.1 F-14, §4.1, §4.2

**背景**

ADR-0023 がタイトルに氏名を要求する。取得元の候補は4つあった。

- 画面からの入力
- People API（`People.People.get('people/me', {personFields:'names'})`）
- Script Properties の固定値
- Script Properties のメールアドレス → 氏名のマップ

制約が2つある。ADR-0014 で OAuth スコープを `calendar` のみと明示宣言しているため、
**追加スコープは自動では入らず、宣言と利用者の再承認を伴う**。
また [#9](https://github.com/diwamotoG/attendancem/issues/9) が控えており、
スクリプト単位で1つしか持てない値は複数ユーザーで破綻する。

**決定**

1. 氏名は画面の入力欄で受け取り、`setDisplayName(name)` でサーバーに渡す
2. 保存先は `PropertiesService.getUserProperties()` の `DISPLAY_NAME`
3. **`punch()` は氏名未設定を拒否する。** 氏名なしのタイトルでは作成しない
4. 検証はサーバー側で行う。前後の空白を除去し、空文字・制御文字・
   `DISPLAY_NAME_MAX_LENGTH` 超過を拒否する。`_` は許容する
5. OAuth スコープは `calendar` のみを維持する（ADR-0014 を変えない）
6. User Properties は GAS エディタから見えないため、`checkConfig()` に氏名を含め、
   プログラム的に設定する `setupDisplayName(name)` を用意する

**理由**

**追加スコープがゼロである**ことが決め手である。`PropertiesService` は既に
`GUEST_EMAILS` で使っており、承認画面も利用者の再承認も一切変わらない。
People API は氏名の正しさを唯一保証できるが、Advanced Service の有効化と
`userinfo.profile` の追加を要し、ADR-0014 が絞ったスコープを広げる。
打刻タイトルの表示名にそこまでの保証は要らないと判断した。

User Properties を選ぶのは、**実行主体ごとのバケットである**ためである。
[#9](https://github.com/diwamotoG/attendancem/issues/9) が `executeAs` を
`USER_ACCESSING` に切り替えた時点で自動的に利用者ごとに分かれ、作り直しが起きない。
ただし現状の `USER_DEPLOYING` では実行主体が常にデプロイ者であり、
**バケットは1つしかない。利用者ごとに分かれるのは `executeAs` の切り替えが前提**である。
本レコードだけで複数ユーザーが解決するわけではない。

保存先をブラウザの `localStorage` にしない理由は、Safari のサイトデータ削除や
ストレージ圧迫で消え、端末を変えるたびに再入力が要るためである。
サーバー側に置けば端末側の状態に依存しない。

氏名未設定で打刻を拒否するのは、ADR-0023 が氏名なしのタイトルを
**打刻として認識しない**ためである。氏名なしで作成すると、
作成した本人が履歴でも状態導出でも見つけられないイベントになる。
「1タップ・1秒・100%成功」（PLAN.md §1）に反して打刻を止めることになるが、
記録として機能しないイベントを作るほうが実害が大きい。

**検討した代替案**

| 案 | 却下理由 |
|----|---------|
| People API で Google アカウントの表示名を自動取得する | 氏名の正しさを唯一保証できるが、Advanced Service の有効化と `userinfo.profile` の追加が要る。ADR-0014 が絞ったスコープを広げ、利用者全員に再承認と承認画面の項目増を強いる。表示名にその代償は見合わない |
| Script Properties に固定値 `USER_DISPLAY_NAME` を置く | 最も安いが、スクリプト単位＝全員共通であり #9 で全員が同じ氏名になる。#9 の時点で作り直しになる |
| Script Properties にメールアドレス → 氏名のマップを置く | 管理者が氏名を採れる利点はあるが、利用者の同定に `userinfo.email` スコープの追加が要る。加えてメンバー追加のたびに GAS エディタでの手編集が必要になり、各自が自分で設定できない |
| 入力値を `localStorage` に持ち `punch()` の引数で毎回送る | サーバーに何も持たない代わりに、サイトデータ削除で消え、端末ごとに再入力が要る。`punch()` のシグネチャも変わり、送信漏れが氏名なしの打刻に直結する |
| `CalendarApp.getDefaultCalendar().getName()` を流用する | 追加スコープは不要だが、個人 Gmail アカウントで返るのが氏名かメールアドレスか実機確認が必要で、確認するまで採否を決められない。氏名が返る保証もなく、返らなければ振り出しに戻る |
| 氏名未設定なら氏名なしのタイトルで打刻する | ADR-0023 が氏名なしを打刻として認識しないため、作成した本人が見つけられないイベントになる。打刻の成功を優先した結果、記録が機能しない |

**影響**

- 良い影響:
  - OAuth スコープ・承認画面・SETUP のプロパティ設定が一切増えない
  - 各自が GAS エディタを開かずに自分で設定・変更できる
  - `executeAs` を切り替えれば、そのまま利用者ごとの氏名になる
- 悪い影響 / トレードオフ:
  - **氏名が自己申告でサーバー側の裏付けがない。** #9 で他人の氏名を入力すれば
    他人名義の打刻が作れる。共通カレンダー上では誤りに気づきにくい
  - **User Properties は GAS エディタの画面から見えない。** Script Properties と異なり
    一覧 UI が存在せず、確認・リセットに関数の実行が要る
  - 氏名未設定という打刻の拒否理由が1つ増える。初回に入力の1ステップが増え、
    SETUP でも打刻ラッパーの実行前に氏名の設定が必要になる
  - 入力値がそのままカレンダーのタイトルに載るため、検証の責任がサーバー側に生じる
- 再検討のトリガー:
  - 氏名の正しさが運用上の問題になった場合（他人名義の打刻、入力ミスの放置）。
    その時点で People API に切り替える
  - #9 が `executeAs: USER_ACCESSING` を採らなかった場合。
    User Properties が利用者ごとに分かれる前提が崩れる

<!-- ここに ADR-0025 以降を追記 -->
