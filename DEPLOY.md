# DEPLOY.md — 変更を本番に反映する手順

コードを直してから、iPhone のホーム画面の打刻画面に反映されるまでの手順書。

初回セットアップは [SETUP.md](./SETUP.md)（AI Agent 向け）または
[README.md](./README.md)（人間向け）を参照する。

---

## 最初に：3つの「push / デプロイ」は別物である

**このプロジェクトには系統の異なる反映先が3つある。混同すると「直したのに変わらない」が起きる。**

```
  ローカルの src/
      │
      ├─ git push ──────────▶  GitHub          … 履歴の保管。GAS には一切影響しない
      │
      └─ npm run push ──────▶  GAS プロジェクト  … エディタ上のコードが変わる
                                    │              @HEAD デプロイには即座に反映される
                                    │
                                    └─ バージョン作成 + 既存デプロイの更新
                                              ▼
                                        本番 Web App URL   … ホーム画面のアイコンが指す先
```

| 操作 | 変わるもの | 変わらないもの |
|------|-----------|---------------|
| `git push` | GitHub のリポジトリ | GAS 上のコード、本番 URL の中身 |
| `npm run push` | GAS エディタのコード、`@HEAD` デプロイ | **本番 URL の中身** |
| バージョン作成＋デプロイ更新 | **本番 URL の中身** | URL そのもの（変わらないことが重要） |

`npm run push` は「デプロイ」ではない。**ソースを GAS プロジェクトへ転送するだけ**である。
本番に出すには、その後にバージョンを切ってデプロイを更新する必要がある。

### デプロイは2つある

```bash
npm run deployments
```

```
- AKfycbws4jH...  @HEAD              ← テスト用。常に最新コードを返す
- AKfycbyMVeb...  @1 - attendancem   ← 本番。バージョン1に固定されている
```

| デプロイ | URL の末尾 | 用途 |
|---------|-----------|------|
| `@HEAD` | `/dev` | 動作確認用。`npm run push` した内容が即座に反映される。**自分しか開けない** |
| バージョン固定（`@1` など） | `/exec` | 本番。ホーム画面のアイコンが指す先。明示的に更新するまで内容が変わらない |

この分離は意図的である。`@HEAD` で壊れたコードを確認しても、本番の打刻は壊れない。

---

## 通常のリリース手順

### 1. コードを直す

`src/` を編集する。仕様に関わる変更なら、**先に PLAN.md を直す**（[CLAUDE.md](./CLAUDE.md) ルール1）。

### 2. GitHub にコミットする

```bash
git add -A
git commit -m "変更内容"
git push
```

GAS への反映とは独立している。**この時点では本番も GAS エディタも変わっていない。**

### 3. GAS プロジェクトへ転送する

```bash
npm run push -- --force
```

`--force` はリモートのマニフェスト（`appsscript.json`）を上書きするために必要。

**検証**: 4ファイルすべてが push されたこと。

```
Pushed 4 files.
└─ src/appsscript.json
└─ src/Code.gs
└─ src/Config.gs
└─ src/index.html
```

### 4. `@HEAD` で動作確認する

`npm run open` でエディタを開き、**デプロイ > デプロイをテスト** の URL（`/dev` で終わる）を開く。
ここには手順3の内容が既に反映されている。

打刻を伴う確認をした場合、**作成された予定をカレンダーから削除する**。
招待先にも通知が飛ぶため、テストのつもりの打刻を残さない。

### 5. 本番デプロイを更新する

**GAS エディタで行う（正の手順）**

1. **デプロイ > デプロイを管理**
2. 対象デプロイ（`attendancem`）の **鉛筆アイコン**
3. バージョンを **「新バージョン」** に変更
4. 説明を入力して **「デプロイ」**

> ⚠️ **「新しいデプロイ」を使わないこと。**
> 新規作成すると **URL が変わり**、iPhone のホーム画面アイコンが古いバージョンを
> 指したままになる。既存デプロイの編集で URL を固定する（[ADR-0016](./ADR.md#adr-0016)）。

**clasp で行う（代替手順・本プロジェクトでは未実行）**

```bash
npm run deployments                                    # deploymentId を確認
npm run clasp -- create-version "変更内容"              # 新バージョンを作る
npm run clasp -- redeploy <deploymentId> -V <番号> -d "変更内容"
```

`redeploy`（`update-deployment`）は既存デプロイを更新するため URL は変わらない。
**この手順は本プロジェクトでまだ実行していない。** 初回は GUI で行い、
clasp 版を使うなら実行後に `npm run deployments` でバージョンが上がったことを確認すること。

### 6. 本番で確認する

iPhone のホーム画面アイコンから開き、変更が反映されていることを確認する。

反映されていない場合は Safari のキャッシュを疑う前に、まず
`npm run deployments` でデプロイのバージョンが上がっているかを見る。

---

## ロールバックする

バージョンは GAS 側に残っているため、デプロイを過去バージョンに向け直せば戻せる。

```bash
npm run clasp -- list-versions        # 戻したいバージョン番号を確認
```

GAS エディタの **デプロイ > デプロイを管理 > 鉛筆アイコン** で、
バージョンのプルダウンから過去のバージョンを選んで「デプロイ」。

**ローカルのコードは戻らない。** 必要なら `git` 側も合わせて戻すこと。

```bash
git log --oneline          # 戻したいコミットを確認
git revert <コミット>       # 履歴を残して打ち消す
```

---

## 設定変更はデプロイ不要

| 変更 | 必要な操作 |
|------|-----------|
| 招待先を変える（`GUEST_EMAILS`） | **スクリプト プロパティを書き換えるだけ。デプロイ不要**（[ADR-0009](./ADR.md#adr-0009)） |
| 招待メールを止める（`PUNCH_TYPES[type].sendInvites`） | `src/Config.gs` の変更 → 手順3〜5が必要。単一フラグ `SEND_INVITES` は廃止済み（[ADR-0020](./ADR.md#adr-0020)） |
| 連打しきい値・状態導出の窓（`MIN_PUNCH_INTERVAL_MS` / `STATE_LOOKBACK_DAYS`） | `src/Config.gs` の変更 → 手順3〜5が必要 |
| タイムゾーン・アクセス範囲 | `src/appsscript.json` の変更 → 手順3〜5が必要。`--force` 必須 |

招待先の変更にデプロイが要らないのは、設定値をコードではなく
Script Properties に置いているため（ADR-0009）。

---

## トラブルシュート

| 症状 | 原因と対処 |
|------|-----------|
| コードを直したのに本番が変わらない | 手順5をやっていない。`npm run push` は本番に出す操作ではない |
| `@HEAD` では直っているが本番が古い | 同上。デプロイのバージョンが上がっていない |
| URL が変わってホーム画面のアイコンが古いまま | 「新しいデプロイ」を作ってしまった。既存デプロイを更新し直し、ホーム画面のアイコンを作り直す |
| `appsscript.json` の変更が反映されない | `--force` を付けていない。`npm run push -- --force` |
| 打刻時刻が9時間ずれた | `src/appsscript.json` の `timeZone` が壊れている。[SETUP.md](./SETUP.md) ステップ4の検証ゲートを参照 |
| 他人が本番 URL を開けてしまう | デプロイのアクセス設定が「自分のみ」でない。ADR-0002 の前提が崩れている。即座に修正する |
| `Not logged in.` | 素の `npx clasp` を叩いている。npm scripts 経由で実行する |

---

## リリース前チェックリスト

- [ ] 仕様が変わるなら PLAN.md を先に更新した（CLAUDE.md ルール1）
- [ ] 決定が変わるなら ADR.md に追記した（ルール2）
- [ ] GitHub にコミット済み
- [ ] `npm run push -- --force` で4ファイル反映済み
- [ ] `@HEAD` で動作確認し、テストで作成した予定を削除した
- [ ] **既存デプロイを「新バージョン」で更新した**（新しいデプロイを作っていない）
- [ ] ホーム画面のアイコンから開いて反映を確認した
