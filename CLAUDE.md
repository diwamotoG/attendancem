# attendancem

勤怠打刻ツール。Google Apps Script Web App として実装する。

## ドキュメント

| ファイル | 役割 |
|---------|------|
| `PLAN.md` | 要件・仕様・実装計画。**実装の唯一の正** |
| `ADR.md` | 設計上・実装上の意思決定記録 |

実装に着手する前に、該当箇所を必ず PLAN.md で確認する。
設計判断の理由を知りたいときは ADR.md を先に読む。既に検討して却下した案が記録されている。

---

## ルール1: 実装が PLAN.md と食い違うなら、実装ではなく PLAN.md を直す

PLAN.md に書かれていない実装、または PLAN.md と異なる実装をするときは、
**実装より先に PLAN.md を更新する。**「あとでまとめて直す」は禁止。

| 該当する変更 | 対応 |
|-------------|------|
| 仕様・インターフェース・スコープが変わる | PLAN.md を更新する |
| 設計方針・採用技術が変わる | PLAN.md を更新する |
| テスト観点が増減する | PLAN.md を更新する |
| 変数名・関数分割・コード整理など、PLAN.md に記載のない実装詳細 | 更新不要 |

判断に迷ったら更新する側に倒す。ドキュメントと実装の乖離が最大のコストになる。

## ルール2: PLAN.md の変更で「決定」が変わったなら、同じ作業の中で ADR.md も更新する

ADR.md の更新は次のいずれかの形をとる。

- 新しい ADR レコードを連番で追記する（テンプレートは ADR.md 冒頭）
- 既存レコードのステータスを `置換済み（ADR-00XX により置換）` に変更し、
  置き換える新レコードを追記する

**判定基準は「決定が変わったか」であり、「ファイルが変わったか」ではない。**
以下は ADR 更新の対象外とする。

- 誤字・表現の修正
- チェックボックスの進捗更新
- 決定内容を変えない補足の追記

この線引きは ADR.md 冒頭の運用ルール9（記録対象は後から変更するのが高くつく判断に限る）に対応する。
何でも記録するとノイズが増え、ADR 全体の価値が下がる。

## ルール3: ADR.md の過去レコードは書き換えない

追記のみ。既存レコードに対して許される唯一の編集はステータス行の更新である。
決定を覆すときは、旧レコードを書き換えず新レコードを起こす。

詳細な運用ルールとレコードのテンプレートは ADR.md 冒頭に記載してある。

---

## 実装時の注意

- タイムゾーンは `Asia/Tokyo` を明示する。暗黙の既定に依存しない（ADR-0007）
- 招待先メールアドレスをソースコードに書かない。Script Properties から読む（ADR-0009）
- `punch()` は例外を投げず `{ok: boolean}` で結果を返す（PLAN.md §4.1）
