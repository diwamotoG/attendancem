# attendancem — 実装計画書

最終更新: 2026-08-05（打刻機能改修の仕様確定 → [#2](https://github.com/diwamotoG/attendancem/issues/2)）

---

## 1. 目的と背景

自分の稼働時間（出社 / 退社 / 休憩）を、ボタン1タップで Google カレンダーに記録し、
関係者2名に自動で共有する。出社・退社は**リアルとリモートを区別する**。

現状は手動でカレンダーに予定を作成しているか、そもそも記録が残っていない。
1日数回・毎営業日という高頻度の定型操作であるため、**1タップ・1秒・100%成功**が
最重要の品質要件になる。

### なぜ AI / MCP ではなくツールを作るのか

claude.ai / ChatGPT の Google Calendar コネクタを使えば開発ゼロで同じことはできるが、
毎日繰り返す定型操作に LLM を挟むと以下が劣化する。

- 応答まで数秒〜数十秒かかる（打刻に求められるのは1秒）
- タイトルの表記ゆれ・招待者の入れ忘れが起きうる（決定性がない）
- 失敗が「たぶん成功した」で終わりやすい
- トークン課金が発生する

**実行系は決定的なコード、分析系は AI** という分担を採る。
月次集計・レポート生成は本ツールでは実装せず、AI に任せる（→ スコープ外）。

詳細な判断根拠は [ADR.md](./ADR.md) を参照。

---

## 2. 要件

### 2.1 機能要件

| ID | 要件 | 優先度 |
|----|------|--------|
| F-1 | 「リアル出社」「リモート出社」ボタン押下で、自分のデフォルトカレンダーに対応するタイトルの予定を現在時刻で作成する | 必須 |
| F-2 | 「リアル退社」「リモート退社」ボタン押下で、同様に予定を作成する | 必須 |
| F-3 | 「休憩開始」「休憩終了」ボタン押下で、同様に予定を作成する | 必須 |
| F-4 | 出社・退社の予定に、あらかじめ設定した2名の Gmail アカウントを招待者として追加し、招待メールを送信する | 必須 |
| F-5 | 休憩の予定にも同じ2名を招待者として追加するが、招待メールは送信しない（→ [ADR-0020](./ADR.md#adr-0020)） | 必須 |
| F-6 | 同一日に出社・退社ペアが複数回あってよい。退社した後、同じ日に再度出社できる | 必須 |
| F-7 | 現在の勤務状態から遷移できない打刻はエラーを返し、イベントを作成しない（→ [ADR-0018](./ADR.md#adr-0018)） | 必須 |
| F-8 | 直前の打刻から `MIN_PUNCH_INTERVAL_MS` 未満の打刻は、種別を問わずエラーを返す | 必須 |
| F-9 | 画面起動時に当日の打刻履歴を、種別と時刻の一覧として表示する | 必須 |
| F-10 | 現在の勤務状態から押せないボタンは非活性にする | 必須 |
| F-11 | 打刻の成否をユーザーに明示的にフィードバックする | 必須 |
| F-12 | 招待先メールアドレスをコード変更なしに設定・変更できる | 推奨 |

**要件 ID について**: 2026-08-05 の打刻機能改修で ID を振り直した。
ADR.md の過去レコードが参照する `F-5` などは**改訂前の番号**を指す。
過去の ADR レコードは書き換えない方針のため（CLAUDE.md ルール3）、この対応表を残す。

| 改訂前 | 改訂後 |
|--------|--------|
| F-1 / F-2（開始・終了） | F-1 / F-2 / F-3（6種別に分割） |
| F-3 / F-4（招待・メール送信） | F-4 / F-5（種別ごとにメール送信可否を分離） |
| F-5（同日の二重打刻防止） | **廃止**。F-6 / F-7 / F-8 が置き換える |
| F-6（当日の打刻状況表示） | F-9 |
| F-7（打刻済みボタンの非活性） | F-10（状態遷移ベースに変更） |
| F-8 / F-9 | F-11 / F-12 |

### 2.2 非機能要件

| ID | 要件 | 目標値 |
|----|------|--------|
| N-1 | 打刻完了までの応答時間 | 3秒以内 |
| N-2 | 操作手数 | ホーム画面から2タップ以内（起動 + 打刻） |
| N-3 | 運用コスト | 0円 |
| N-4 | 認証の再ログイン頻度 | 定期的な再認可が不要であること |
| N-5 | 利用端末 | iPhone Safari を主、PC ブラウザを従 |
| N-6 | 記録のタイムゾーン | Asia/Tokyo に固定 |
| N-7 | 自分以外からアクセスできないこと | Google アカウント認証で制限 |

### 2.3 スコープ外（今回作らないもの）

- 稼働時間の集計・レポート出力（AI + カレンダー参照で代替する）
- 打刻の修正・削除 UI（Google カレンダー本体で直接編集する）
- 打刻忘れの通知・リマインダー（将来の拡張候補）
- 複数ユーザー対応 / 権限管理
- 打刻データの外部 DB への永続化（カレンダーが唯一の記録先）
- **2日以上にわたる連続勤務**。状態導出の窓は前日 0:00 までであり、それ以前に遡らない
  （→ [ADR-0018](./ADR.md#adr-0018)）。1晩を跨ぐ勤務（22:00 出社 → 翌 01:00 退社）は
  スコープ**内**であり、T-14 / T-15 で検証する

> 「休憩時間の記録」は 2026-08-05 の改修でスコープ**内**に移した（F-3 / F-5）。

### 2.4 打刻種別と勤務状態

#### 打刻種別（6種）

`type` はクライアントから `punch()` に渡す識別子。`title` はカレンダー上のイベント名であり、
**種別判定のキーでもある**。過去の打刻との一致に影響するため、確定後は変更しない。

| `type` | `title` | 招待メール |
|--------|---------|-----------|
| `officeIn` | `リアル出社` | 送る |
| `remoteIn` | `リモート出社` | 送る |
| `officeOut` | `リアル退社` | 送る |
| `remoteOut` | `リモート退社` | 送る |
| `breakIn` | `休憩開始` | **送らない** |
| `breakOut` | `休憩終了` | **送らない** |

改訂前の `開始` / `終了` は、いずれの種別にも一致しない。
既存の打刻イベントは履歴表示にも状態導出にも現れなくなる（→ [ADR-0017](./ADR.md#adr-0017)）。

#### 勤務状態と遷移

状態は打刻イベント列から**毎回導出する**。状態そのものを永続化しない
（カレンダーが唯一の記録先であるという方針を維持する）。

```
              officeIn / remoteIn
   ┌────────┐ ──────────────────▶ ┌────────┐
   │ 勤務外 │                      │ 勤務中 │
   └────────┘ ◀────────────────── └────────┘
              officeOut/remoteOut    │    ▲
                                     │    │
                             breakIn │    │ breakOut
                                     ▼    │
                                  ┌────────┐
                                  │ 休憩中 │
                                  └────────┘
```

| 現在の状態 | 許可される打刻 | 拒否される打刻 |
|-----------|--------------|--------------|
| 勤務外（初期状態 / 退社済み） | `officeIn` `remoteIn` | 退社系・休憩系すべて |
| 勤務中 | `officeOut` `remoteOut` `breakIn` | 出社系・`breakOut` |
| 休憩中 | `breakOut` | それ以外すべて |

- 「勤務外 → 出社」は**何度でも遷移できる**。これが F-6（同日複数ペア）の実体である
- 出社の形態（リアル / リモート）と退社の形態は**一致しなくてよい**。
  リアル出社してリモート退社する運用がありうるため、組み合わせを制約しない
- **休憩中の退社は拒否する。** 先に `breakOut` を打つ必要がある。
  休憩を閉じないまま退社すると、招待先が実働時間を計算できなくなるため

#### 状態導出の窓

`getTodayEvents()` は当日 0:00〜24:00 を見るが、この窓で状態を導出すると
日を跨いだ勤務の退社が「勤務外からの退社」として拒否される。

状態導出は **前日 0:00 から現在まで**のイベント列に対して行う。
画面に表示する履歴は従来どおり当日分のみとする（→ [ADR-0018](./ADR.md#adr-0018)）。

窓は「現在時刻から N 時間前」ではなく **カレンダー日付の境界**で切る。
呼び出し時刻によって窓の起点が動くと、同じ打刻列でも時刻次第で状態が変わってしまうため。
境界計算は `getTodayEvents()` と同じく Asia/Tokyo で解釈される（ADR-0007）。

---

## 3. アーキテクチャ

### 3.1 構成図

```
  iPhone / PC ブラウザ
        │
        │ HTTPS (Google アカウント認証)
        ▼
  ┌─────────────────────────────────┐
  │  Google Apps Script Web App     │
  │                                 │
  │  index.html  ──google.script.run──▶ Code.gs      │
  │  (UI)                              (punch/status)│
  │                                        │         │
  │                                 Config.gs        │
  │                                 (Script Properties 読み出し) │
  └────────────────────────────────────────┼─────────┘
                                           │ CalendarApp
                                           ▼
                              自分の Google カレンダー
                                           │ 招待
                                           ▼
                              招待先 Gmail × 2（招待メール送信）
```

### 3.2 主要な設計判断（要約）

| 項目 | 決定 | ADR |
|------|------|-----|
| 実行基盤 | Google Apps Script Web App | [ADR-0001](./ADR.md#adr-0001) |
| 認証 | Web App「自分として実行」に委譲（独自 OAuth なし） | [ADR-0002](./ADR.md#adr-0002) |
| イベント設計 | 打刻ごとに独立した1件（帯にしない） | [ADR-0003](./ADR.md#adr-0003) |
| 打刻種別 | 6種別を独立したイベントタイトルで表現する | [ADR-0017](./ADR.md#adr-0017) |
| 招待通知 | 出社・退社は送信、休憩は招待のみで送信しない | [ADR-0020](./ADR.md#adr-0020) |
| フロント配信 | HtmlService で同居配信 + `google.script.run` | [ADR-0005](./ADR.md#adr-0005) |
| 同日複数打刻 | 重複禁止を廃止し、状態遷移チェック + LockService で守る | [ADR-0018](./ADR.md#adr-0018) |
| タイムゾーン | `appsscript.json` で Asia/Tokyo 固定 | [ADR-0007](./ADR.md#adr-0007) |
| イベント長 | 1分 | [ADR-0008](./ADR.md#adr-0008) |
| 設定値の保持 | Script Properties | [ADR-0009](./ADR.md#adr-0009) |
| 誤タップ対策 | 確認ダイアログを設けず、状態遷移チェックと連打しきい値で対応 | [ADR-0019](./ADR.md#adr-0019) |
| 反映手段 | clasp をプロジェクトローカルに導入し、認証情報もリポジトリ内に置く | [ADR-0012](./ADR.md#adr-0012) |
| OAuth スコープ | `calendar` のみを明示宣言する | [ADR-0014](./ADR.md#adr-0014) |
| セットアップ手順 | AI Agent 向けに SETUP.md を用意し、マニフェスト上書きを回避で防ぐ | [ADR-0015](./ADR.md#adr-0015) |
| 本番反映 | 既存デプロイをバージョン更新して Web App URL を固定する | [ADR-0016](./ADR.md#adr-0016) |

### 3.3 ファイル構成

```
attendancem/
├── PLAN.md            # 本ドキュメント
├── ADR.md             # アーキテクチャ決定記録
├── README.md          # 人間向けのセットアップ・デプロイ手順・トラブルシュート
├── SETUP.md           # AI Agent 向けセットアップ手順書（→ ADR-0015）
├── DEPLOY.md          # 本番反映・ロールバック手順（→ ADR-0016）
├── CLAUDE.md          # ドキュメント同期の開発ルール
├── .gitignore
├── .gas-init/         # clasp create のひな形 clone 先。作成後に削除する（→ ADR-0015）
├── package.json       # clasp をローカル導入するためだけの定義（→ ADR-0012）
├── package-lock.json
├── node_modules/      # clasp 本体。コミットしない
├── .clasp.json        # scriptId と rootDir。clasp create が生成。コミットしない
├── .clasprc.json      # clasp の OAuth トークン。コミットしない（→ ADR-0012）
├── .claude/
│   ├── settings.json           # Stop hook の登録
│   └── hooks/
│       └── check-docs-sync.sh  # PLAN.md / ADR.md の同期チェック
└── src/               # clasp の rootDir。GAS に反映されるのはこの配下だけ
    ├── appsscript.json  # マニフェスト（TZ・スコープ・Web App 設定）
    ├── Config.gs        # 設定値の読み出しと初期セットアップ関数
    ├── Code.gs          # doGet / punch / getStatus などサーバーロジック
    └── index.html       # UI（HTML + CSS + クライアント JS を1ファイルに同梱）
```

ルート直下の `package.json` / `node_modules/` は **開発端末で clasp を動かすためだけ**に存在し、
GAS のランタイムには一切関与しない。`rootDir` が `./src` であるため push 対象にもならない。

---

## 4. インターフェース仕様

### 4.1 サーバー関数（`google.script.run` から呼ぶ）

#### `getStatus() → Status`

当日の打刻履歴と、現在の勤務状態を返す。画面起動時と各打刻の直後に呼ぶ。

```js
// Status
{
  date: "2026-08-05",

  // 当日の打刻履歴。時刻の昇順。1件も無ければ空配列
  punches: [
    { type: "officeIn",  title: "リアル出社", time: "09:02" },
    { type: "breakIn",   title: "休憩開始",   time: "12:05" },
    { type: "breakOut",  title: "休憩終了",   time: "12:48" },
    { type: "officeOut", title: "リアル退社", time: "18:31" },
    { type: "remoteIn",  title: "リモート出社", time: "20:10" }
  ],

  // 前日 0:00 から現在までの打刻列から導出した現在の状態（§2.4）
  state: "offDuty" | "onDuty" | "onBreak",

  // state から遷移できる type の配列。UI のボタン活性制御はこれだけを見る
  allowed: ["officeOut", "remoteOut", "breakIn"],

  guests: ["a@gmail.com", "b@gmail.com"],  // 表示確認用
  error: null | "招待先が未設定です。..."   // 設定不足・読み込み失敗の内容
}
```

`getStatus()` は例外を投げない。設定不足やカレンダー読み込み失敗は `error` に詰め、
呼び出し側が常に Status を受け取れるようにする（§4.2 の「明示的なエラーメッセージを返す」の実現方法）。

**`allowed` をサーバーが返す理由**: 遷移表（§2.4）をクライアントにも実装すると、
状態遷移の定義がサーバーとクライアントの2箇所に重複する。
UI は `allowed` に含まれるかどうかだけを見て活性制御し、遷移の判断を持たない。

`punches` は当日分のみだが、`state` / `allowed` は前日 0:00 からの列で導出する。
両者の窓が異なるため、日跨ぎ勤務では「履歴が空なのに `state` が `onDuty`」という
組み合わせが正常に起こりうる。UI はこれを異常として扱わないこと。

#### `punch(type) → Result`

`type` は §2.4 の6種別のいずれか。

```js
// Result（成功）
{ ok: true,  message: "「リアル出社」を 09:02 で登録しました", status: Status }

// Result（失敗：状態遷移が不正）
{ ok: false, error: "休憩中です。先に「休憩終了」を打刻してください", status: Status }

// Result（失敗：連打しきい値）
{ ok: false, error: "直前の打刻から1分経過していません（09:02 に「リアル出社」）", status: Status }

// Result（失敗：不正な種別）
{ ok: false, error: "不正な打刻種別です: xxx", status: Status }
```

例外は投げず、`ok` フラグで表現する（`google.script.run` の
`withFailureHandler` は通信断・スクリプトエラーのみに使う）。

失敗時も `status` は必ず含める。UI が拒否理由の表示と同時に、
最新の状態でボタン活性を再計算できるようにするため。

### 4.2 Script Properties

| キー | 例 | 用途 |
|------|-----|------|
| `GUEST_EMAILS` | `a@gmail.com,b@gmail.com` | 招待先。カンマ区切り |

未設定時は `punch()` / `getStatus()` が明示的なエラーメッセージを返す。

### 4.3 `Config.gs` の定数

挙動を切り替える値は `Config.gs` に集約し、`Code.gs` に散らさない（既存方針の継続）。

| 定数 | 値 | 用途 |
|------|-----|------|
| `PUNCH_TYPES` | §2.4 の6種別 | `type` → `{title, sendInvites, from[], to}` の定義。**遷移表もここに持たせる** |
| `MIN_PUNCH_INTERVAL_MS` | `60 * 1000` | 連打しきい値。この時間内の打刻は種別を問わず拒否（F-8） |
| `STATE_LOOKBACK_DAYS` | `1` | 状態導出の窓を何日前の 0:00 まで遡るか（§2.4）。**日数であり時間幅ではない**。`0` にすると当日 0:00 起点となり、日跨ぎ勤務の退社が拒否される |
| `SEND_INVITES` | — | **廃止。** 種別ごとの `PUNCH_TYPES[type].sendInvites` に移す（→ [ADR-0020](./ADR.md#adr-0020)） |
| `EVENT_DURATION_MS` | `60 * 1000` | 変更なし（ADR-0008） |
| `TIME_ZONE` | `Asia/Tokyo` | 変更なし（ADR-0007） |
| `LOCK_TIMEOUT_MS` | `10 * 1000` | 変更なし。排他制御は維持する |

遷移表を `PUNCH_TYPES` に同居させるのは、種別の追加時に
定義と遷移の更新漏れが起きないようにするため。

---

## 5. 実装フェーズ

### Phase 1 — 骨組みとサーバーロジック

- [x] `src/appsscript.json`：タイムゾーン `Asia/Tokyo`、OAuth スコープ、Web App 設定
- [x] `src/Config.gs`：`getGuestEmails()`、`setupGuests(csv)` ヘルパー、挙動を切り替える定数の集約
- [x] `src/Code.gs`：`doGet()`、`punch(type)`、`getStatus()`、内部ヘルパー
  - 当日イベント取得 `getTodayEvents(now)`
  - 打刻イベント検索 `findPunchEvent(events, title)` / `findPunchTime(events, title)`
  - 時刻フォーマット `formatTime(date)`
  - LockService による排他
- [x] 動作確認用ラッパー `checkConfig()` / `testPunchStart()` / `testPunchEnd()`
- [x] GAS プロジェクトへの反映と実機確認（2026-08-05 完了。T-1 / T-2 / T-3 / T-7 合格）

**完了条件**: GAS エディタから `testPunchStart()` を実行し、カレンダーに
「開始」が作成され、2名に招待メールが届く。

GAS エディタの「実行」は関数に引数を渡せないため、`punch('start')` を直接実行できない。
検証は引数なしのラッパー関数 `testPunchStart()` / `testPunchEnd()` 経由で行う。

### Phase 2 — UI

- [x] `src/index.html`：
  - 当日の打刻状況表示（開始 / 終了）
  - 「開始」「終了」の大型ボタン（min-height 88px、間隔 32px）
  - 打刻済みボタンの非活性化
  - 送信中のローディング表示（二重タップ抑止）
  - 成功 / 失敗のトースト表示
  - 設定不足・読み込み失敗のバナー表示（`Status.error`）
  - `visibilitychange` での状態再取得（日付跨ぎ・復帰時のずれ防止）
  - `prefers-color-scheme` / `prefers-reduced-motion` 対応
- [x] iPhone Safari での実機確認（2026-08-05 完了）

**完了条件**: iPhone Safari から実際に打刻でき、状態表示が正しく更新される。→ 達成

### Phase 3 — デプロイと運用整備

- [x] `README.md`：セットアップ手順、設定変更手順、トラブルシュート、誤タップ時の復旧手順
- [x] clasp をプロジェクトローカルに導入（`package.json` / npm scripts、グローバルインストールなし → [ADR-0012](./ADR.md#adr-0012)）
- [x] `npm run login` で認証（`.clasprc.json` をリポジトリ内に生成）
- [x] `npm run create` で GAS プロジェクトを作成し `npm run push` で反映
- [x] Script Properties に `GUEST_EMAILS` を設定（検証用に1件。本番運用前に2名へ差し替える）
- [x] Web App デプロイ（実行ユーザー: 自分 / アクセス: 自分のみ）
- [x] iPhone のホーム画面に Web App URL を追加

**完了条件**: ホーム画面アイコンから2タップで打刻が完了する。→ 達成

**2026-08-05: MVP 完成。** Phase 1〜3 の全項目が完了し、日常運用を開始できる状態にある。
残る作業は運用上の設定のみ（`GUEST_EMAILS` を検証用1件から本番の2名へ差し替える）。

### Phase 4 — 打刻機能改修（[#1](https://github.com/diwamotoG/attendancem/issues/1)）

出社形態の区別・休憩打刻の新設・同日複数ペアの許可。
本ドキュメントの §2.1 / §2.3 / §2.4 / §4.1 / §4.3 / §6 が改訂の対象。

- [x] 仕様・設計の決定（本改訂と ADR-0017〜0020） — [#2](https://github.com/diwamotoG/attendancem/issues/2)
- [ ] サーバー実装 `Code.gs` / `Config.gs`、および `testTransitions()`（§6.1） — [#3](https://github.com/diwamotoG/attendancem/issues/3)
- [ ] UI 実装 `index.html`（6ボタンのレイアウト決定を含む） — [#4](https://github.com/diwamotoG/attendancem/issues/4)
- [ ] T-1〜T-20 の実行、デプロイ、README / SETUP 更新 — [#5](https://github.com/diwamotoG/attendancem/issues/5)

**完了条件**: 同一日に「出社 → 退社 → 出社 → 退社」が記録でき、
休憩の招待メールが飛ばないことを実機で確認できている。

`#3` が単独で main に入った時点では `index.html` が旧 `Status` を参照して壊れる。
push とデプロイは分離されているため（DEPLOY.md）、`#4` が揃うまで本番反映しないこと。

---

## 6. テスト計画

検証は2層に分ける（→ [ADR-0021](./ADR.md#adr-0021)）。

| 層 | 対象 | 手段 |
|----|------|------|
| §6.1 自動 | 勤務状態の遷移表と状態導出 | GAS エディタから `testTransitions()` を実行 |
| §6.2 手動 | 上記以外すべて（カレンダー連携・メール・UI・認証） | T-1〜T-20 を実機で実行 |

外部のテストフレームワーク・CI は導入しない。
自動化するのは**カレンダーにも UI にも触れずに検証できる純粋なロジックだけ**である。

### 6.1 遷移表の自動テスト

遷移の判定を副作用のない関数として切り出し、GAS エディタから網羅的に検証する。

| 関数 | 入力 | 出力 | 純粋性 |
|------|------|------|--------|
| `getAllowedTypes(state)` | `'offDuty' \| 'onDuty' \| 'onBreak'` | 許可される `type` の配列 | 純粋 |
| `deriveState(types)` | 打刻 `type` の配列（時刻昇順） | 到達した状態 | 純粋 |

いずれも `CalendarApp` / `PropertiesService` / `new Date()` に触れない。
これが自動テストを成立させる条件であり、**#3 の実装はこの分離を守ること**。

`testTransitions()` は引数なしで GAS エディタから実行でき、
最低限つぎを検証して失敗件数を `Logger` に出す。

- **3状態 × 6種別 = 18通りすべて**について、`getAllowedTypes()` の結果が §2.4 の遷移表と一致する
- `deriveState([])` が `offDuty` を返す（初期状態）
- 同日複数ペア: `[officeIn, officeOut, remoteIn, remoteOut]` が `offDuty` で終わる
- 休憩の入れ子: `[officeIn, breakIn, breakOut, officeOut]` が `offDuty` で終わる
- 途中終了: `[officeIn, breakIn]` が `onBreak` を返す

網羅の対象は遷移表そのものである。**しきい値・ロック・カレンダー I/O は
`testTransitions()` の責務ではない**（それぞれ T-11 / T-12 / T-1 が受け持つ）。

### 6.2 手動検証

| # | 観点 | 手順 | 期待結果 |
|---|------|------|----------|
| T-1 | 出社打刻 | 勤務外の状態で「リアル出社」を押す | `リアル出社` が現在時刻で作成、2名に招待メール |
| T-2 | 退社打刻 | 勤務中の状態で「リアル退社」を押す | `リアル退社` が作成、2名に招待メール |
| T-3 | リモート種別 | 「リモート出社」「リモート退社」を押す | それぞれ対応するタイトルで作成される |
| T-4 | 休憩打刻 | 勤務中に「休憩開始」→「休憩終了」を押す | 両イベントが作成される |
| T-5 | 休憩のメール抑止 | T-4 の直後に招待先の受信箱を確認 | **招待メールは届かない**。招待先のカレンダーには予定が表示される |
| T-6 | 同日複数ペア | 出社 → 退社 → 出社 → 退社 を1日で実行 | 4件すべて作成され、2巡目の出社が拒否されない |
| T-7 | 形態の混在 | リアル出社 → リモート退社 | 拒否されず、両方作成される |
| T-8 | 遷移違反（退社） | 勤務外の状態で「リアル退社」を押す | イベントが作成されず、理由つきエラーが返る |
| T-9 | 遷移違反（休憩） | 休憩中に「リアル退社」を押す | 拒否され、「先に休憩終了を」と案内される |
| T-10 | 遷移違反（二重出社） | 勤務中に「リモート出社」を押す | 拒否される |
| T-11 | 連打しきい値 | 出社の直後（1分以内）に退社を押す | 拒否される（F-8） |
| T-12 | 連打耐性 | 同じボタンを素早く3回タップ | イベントは1件のみ（LockService） |
| T-13 | 状態復元 | 打刻後にページを再読み込み | 履歴が全件表示され、`allowed` に沿ってボタンが活性/非活性 |
| T-14 | 日付跨ぎ（状態） | 22:00 に出社し、翌 01:00 に退社を押す | 退社が**受理される**（状態導出の窓が前日 0:00 まで） |
| T-15 | 日付跨ぎ（表示） | T-14 の翌 01:00 時点の画面 | 履歴は当日分のみ。`state` は `onDuty` |
| T-16 | タイムゾーン | 打刻時刻とカレンダー表示を照合 | JST でずれがない |
| T-17 | 設定未投入 | `GUEST_EMAILS` 未設定で開く | 設定を促すエラーが表示され、打刻できない |
| T-18 | アクセス制限 | 別 Google アカウントで URL を開く | アクセス拒否される |
| T-19 | モバイル表示 | iPhone Safari で開く | 横スクロールなし、6ボタンが押しやすい |
| T-20 | 打刻外イベントの無視 | カレンダーに `開始` や `定例MTG` を手動作成した状態で開く | 履歴に現れず、状態導出にも影響しない |

**実待機を避ける観点**: T-6（同日複数ペア）と T-11（連打しきい値）は
`MIN_PUNCH_INTERVAL_MS`（既定 60 秒）に阻まれ、素直に実施すると打刻ごとに1分待つことになる。
T-14 / T-15（日付跨ぎ）に至っては実待機が不可能である。

これらは `MIN_PUNCH_INTERVAL_MS` と `STATE_LOOKBACK_DAYS` を一時的に変更するか、
GAS エディタから時刻を注入して検証する。**T-11 だけは既定値のまま実施すること**
（しきい値そのものが検証対象であるため）。手順と使用した値は #5 に記録する。

T-20 は、改訂前の `開始` / `終了` が過去日にしか存在せず当日には現れないため、
手動で同名の予定を当日に作成して検証する。
これは ADR-0006 が「悪い影響」に挙げていた
「同名の予定を別目的で手動作成すると打刻済みと誤判定される」が
解消したことの確認も兼ねる。

T-5（休憩のメール抑止）は**招待先の受信箱を実際に確認する**こと。
`sendInvites: false` はカレンダーへの表示は残すため、
カレンダー側だけを見ても抑止できているか判断できない。

### 6.3 検証状況

**2026-08-05 の改訂により、T-1〜T-20 と `testTransitions()` はすべて未検証である。**

MVP 時点（改訂前の T-1〜T-10）の検証結果は根拠にできない。理由は2つある。

1. 観点そのものが入れ替わっており、番号が一致しない
2. 改訂前の §6 に「UI やイベント検索ロジックを変更した際はこの表を根拠にせず、
   改めて通すこと」と記していた。本改修は**その両方**を変更する

| 観点 | 状況 | 根拠 |
|------|------|------|
| `testTransitions()`（§6.1） | 未検証 | [#3](https://github.com/diwamotoG/attendancem/issues/3) の完了条件に含める |
| T-1〜T-20（§6.2） | 未検証 | Phase 4 の [#5](https://github.com/diwamotoG/attendancem/issues/5) で実施し、この表を結果で置き換える |

参考として、MVP 時点の検証状況を残す（改訂前の番号であることに注意）。

| 改訂前の観点 | 状況 | 根拠 |
|------|------|------|
| 旧 T-1 / T-2 / T-3 / T-7 | 合格 | GAS エディタでの `testPunchStart()` / `testPunchEnd()` の実行ログで確認 |
| 旧 T-5 / T-10 ほか iPhone 側 | 合格 | iPhone Safari での実機確認で確認（個別の実行ログは未記録） |
| 旧 T-4 / T-6 / T-8 / T-9 | 個別の記録なし | 実機確認の中で問題が出なかったことをもって可とした |

---

## 7. リスクと対策

| リスク | 影響 | 対策 |
|--------|------|------|
| 招待メールの通数が増える | 関係悪化 | 休憩はメールを送らない（→ [ADR-0020](./ADR.md#adr-0020)）。同日複数ペアの分は増えるが、増分は実際に中抜けした日に限られる。送信可否は `PUNCH_TYPES` の種別ごとのフラグで切り替えられる |
| 打刻忘れ | 記録の欠落 | 今回はスコープ外。カレンダー本体で後から手修正する。頻発するならリマインダーを追加（§8） |
| **休憩を閉じ忘れたまま退社しようとする** | 打刻できず戸惑う | 状態遷移チェックが拒否し、「先に休憩終了を」と案内する（T-9）。運用で頻発するなら [ADR-0018](./ADR.md#adr-0018) の再検討トリガーに該当する |
| 誤タップで意図しない打刻をし、招待先に誤ったメールが届く | 招待先の混乱 | 状態遷移チェックと連打しきい値で大半を弾く。復旧はカレンダーから予定を削除する手順を README に記載（→ [ADR-0019](./ADR.md#adr-0019)） |
| **ボタンが6つになり、押し間違いの確率が上がる** | 誤打刻の増加 | レイアウト方式を [#4](https://github.com/diwamotoG/attendancem/issues/4) で決定し ADR に残す。ADR-0019 の物理的配置による対策は6ボタンではそのまま成立しない |
| GAS の実行時間・呼び出し回数の割当上限 | 打刻失敗 | 1日数回の呼び出しでは上限に対して桁違いに余裕があり、実質リスクなし |
| デフォルトカレンダーが意図と異なる | 別カレンダーに記録 | Phase 1 完了条件の実機確認で検証。必要なら Config でカレンダー ID を指定可能にする |
| ネットワーク断で打刻が失敗 | 記録漏れ | 失敗を明示的に表示し、リトライを促す。オフラインキューは作らない |

---

## 8. 将来の拡張候補（今回はやらない）

- 打刻忘れリマインダー（GAS の時間主導トリガー）
- iOS ショートカット対応（`doPost` + シークレットトークン方式）
- 出社〜退社を1件のイベントに統合する方式への移行（→ [ADR-0003](./ADR.md#adr-0003) の再検討。
  [ADR-0017](./ADR.md#adr-0017) で再度検討した上で見送っている）
- 日付を跨ぐ勤務への対応（→ [ADR-0018](./ADR.md#adr-0018)。現状は状態導出の窓を前日 0:00 までとして部分対応）
- 改訂前の `開始` / `終了` イベントの移行（現状は放置し、履歴・状態導出のいずれにも現れない）
